# insights.md — client

> Append-only. Add new entries at the bottom of the correct section.
> Discovery bar: "Would a fresh agent save ≥10 minutes from reading this?" If not, skip.
> Format: `**YYYY-MM-DD [Category]** — actionable sentence. \`file:line\``
> See `.claude/skills/engineering-insights/` for full criteria and format rules.

## Patterns
<!-- Reusable approaches that worked in this module. -->
- **2026-07-10 [Pattern]** — New domain hook files (`hooks/eval.ts`, `onboarding.ts`, `skills.ts`, `conventions.ts`, and now `ci.ts`) are not required to be added to `hooks/index.ts`'s barrel, despite its comment claiming every hook "resolves here" — 4 pre-existing domain files were never added, and consumers import them directly (`@/lib/hooks/eval`, etc.). Before assuming a new hook file must be wired into the barrel, grep `hooks/index.ts` for sibling domain files first — omission is an accepted, already-established pattern, not an oversight to fix. `client/src/lib/hooks/index.ts`
- **2026-07-10 [Pattern]** — No existing `hooks/*.ts` file built a GET query string from optional filters before now — every prior `api.get` call either took zero params or inlined a required value via template literal (e.g. `useEvalCompare`'s `` ?a=${a}&b=${b} ``). For `useCiRuns`'s optional `{agentId, repo, status, sinceDays}` filters, `URLSearchParams` (already used elsewhere in the client for parsing, just not inside hooks) with conditional `.set()` per truthy filter is the cleanest fit — reuse this shape for the next hook with optional GET filters instead of manual string concatenation. `client/src/lib/hooks/ci.ts`
- **2026-07-10 [Pattern]** — `ci.json`'s `runs.table` only drafts 6 column headers (`timestamp`, `pullRequest`, `source`, `findings`, `cost`, `status`) even though the `CiRun` contract carries `repo`, `agent`, and `duration_s` as separate fields the CI Runs page must also show — and `ci.json` is a frozen, already-owned dependency file (Step 5), not something a later step should edit just to add headers. Resolution: fold `repo`+`agent` into the "source" cell (repo as the primary line, agent muted underneath) and `cost_usd`+`duration_s` into the "cost" cell (same primary/secondary split), reusing the two-line-cell convention already established by `RunHistory.tsx` (agent name + provider/model) rather than requesting new i18n keys outside your owned paths. Apply the same fold-extra-fields-into-an-existing-header technique whenever a later implementation step needs to render more data points than an earlier, already-drafted i18n contract has headers for. `client/src/app/ci-runs/_components/CiRunsView/_components/RunRow/RunRow.tsx`
- **2026-07-10 [Pattern]** — `client/src/app/agents/[id]/page.tsx`'s `VALID_TABS = TABS.map((t) => t.key)` derives the `?tab=` URL whitelist directly from `AgentEditor/constants.ts`'s `TABS` array (the file's own comment says this was a fix for a past regression where a tab was added to `TABS` but not to a separate hardcoded gate). Adding a new tab to `TABS` (e.g. the CI tab) is self-sufficient for `?tab=<key>` routing to work end-to-end — no companion edit to `page.tsx` is ever needed, and none should be added. `client/src/app/agents/[id]/page.tsx:19`
- **2026-07-10 [Pattern]** — To assert WHICH icon a component rendered in an RTL test when no accessible name/role distinguishes a decorative `lucide-react` `<svg>`, render a second, isolated reference instance of the SAME expected `Icon[name]` component in the same test (`render(<ExpectedIcon />)`) and diff its `svg.getAttribute("class")` against the actual component's icon, instead of hardcoding lucide's kebab-case class name. Immune to lucide-react version bumps and to the aliasing described in the sibling Quirk entry below. `client/src/app/agents/[id]/_components/AgentEditor/_components/CITab/CITab.test.tsx`

## Mistakes
<!-- Failure modes, antipatterns, wrong assumptions. Prioritize this section. -->
- **2025-06-01 [Mistake]** — Hardcoding English strings in JSX bypasses `next-intl` and breaks non-English locales. Add a key to `messages/<locale>/` and use `useTranslations()` in the component — never inline English text, even temporarily.

## Decisions
<!-- Architectural or design choices with the reasoning behind them. -->
- **2025-06-01 [Decision]** — All UI primitives (Button, Card, Badge, Dialog, etc.) live in `src/vendor/ui/`; there is no Shadcn, Radix, or Headless UI dependency. When adding a new primitive, extend `src/vendor/ui/` rather than installing an external component library. `client/src/vendor/ui/`
- **2025-06-01 [Decision]** — No global state store: all server-derived state goes through TanStack Query and local UI state uses `useState`/`useReducer`. To share data between components, use the same QueryKey — React Query deduplicates fetches automatically.
- **2026-07-10 [Decision]** — `useCiRuns` (`client/src/lib/hooks/ci.ts`, Step 5) shipped with `refetchInterval` deliberately "left to the consuming page" per its own plan step's wording, but the hook's actual signature took only `filters` — no options passthrough existed, so a downstream page had no compliant seam to activate polling other than editing the hook itself (the alternative, `queryClient.setQueryDefaults`, is unprecedented anywhere in this codebase and itself would need a paragraph of justification — a smell per this project's own P1 "no load-bearing workarounds" principle). Extended `useCiRuns(filters, options?: { refetchInterval?: number })` additively — existing single-argument callers are unaffected, and the CI Runs page passes `{ refetchInterval: 15_000 }` so React Query's own observer lifecycle (not a manual `setInterval`) stops polling on unmount. When a plan explicitly defers wiring a cross-cutting concern (auto-refresh, cache invalidation, etc.) "to a later step" without the earlier step actually building the seam for it, prefer a small additive signature extension on the shared hook over a workaround in the consuming component — verify zero other consumers exist first (`grep` the hook name) since the file sits outside the later step's own owned-paths list. `client/src/lib/hooks/ci.ts`
- **2026-07-10 [Decision]** — The CI tab's "Fail CI on" control imports `CI_FAIL_ON_VALUES` directly from the sibling `ConfigTab/constants.ts` rather than promoting it to the parent `AgentEditor/constants.ts`, per an explicit plan-step instruction (the owned-paths boundary for that step excluded `ConfigTab/`). This constant now has 2 sibling consumers under the same parent feature — per the frontend-architecture "promotion rule" (2nd consumer → promote), a future cleanup should move it to `AgentEditor/constants.ts` so neither tab reaches into the other's folder. `client/src/app/agents/[id]/_components/AgentEditor/_components/ConfigTab/constants.ts`
- **2026-07-10 [Decision]** — Consolidating `InstallationRow.tsx`'s drifted local status-style map into the CI Runs page's canonical `STATUS_META` required wiring `bg`, not just the `.icon`/`.color` a bug report's fix description literally named — the vendor `Badge` component defaults an omitted `bg` to `var(--bg-hover)`, which coincidentally equals `STATUS_META.skipped_fork.bg` but none of the other 4 statuses' real backgrounds (`succeeded`/`no_findings`→`var(--ok-bg)`, `failed`→`var(--crit-bg)`, `running`→`var(--accent-bg)`); wiring only icon/color would have left 4/5 statuses still visually mismatched against the very page the fix claims to match, silently perpetuating the bug under a narrower reading of the fix. Cross-feature import used to reach `STATUS_META` (`@/app/ci-runs/...` from `@/app/agents/[id]/...`) has 2 existing precedents in this codebase (`ContextTab.tsx`, `ContextDocsList.tsx`) — grep `from "@/app/` before assuming a pragmatic cross-page-feature import is unprecedented. `client/src/app/agents/[id]/_components/AgentEditor/_components/CITab/_components/InstallationRow.tsx`

## Quirks
<!-- Dependency gotchas, env constraints, non-obvious tool or library behavior. -->
- **2025-06-01 [Quirk]** — Next.js 15 changed `params` and `searchParams` to `Promise<{...}>`; always `await params` before destructuring, even in async Server Components. TypeScript catches this only if the page signature is correctly typed.
- **2025-06-01 [Quirk]** — Client tests use vitest + jsdom with `fetch` fully mocked — no running server or Docker required. When a test fails with a 404 or network error, a mock is missing, not the server.
- **2026-06-26 [Quirk]** — `@devdigest/shared` maps in the client to `./src/vendor/shared/index.ts` — a separate copy from `server/src/vendor/shared/`. Whenever a shared contract changes, both copies must be updated in the same commit or the client typecheck fails silently. `client/src/vendor/shared/contracts/`
- **2026-06-27 [Quirk]** — `findings.severity` is stored UPPERCASE in Postgres ('CRITICAL'/'WARNING'/'SUGGESTION'), matching the `Severity` Zod enum. DB aggregation queries must compare with uppercase; the API response exposes lowercase keys (`{ critical, warning, suggestion }`) in the `findings_breakdown` object. Mixing case silently produces zero counts — easy to miss because the query succeeds and the UI renders nothing. `server/src/modules/reviews/repository/run.repo.ts`, `server/src/modules/pulls/routes.ts`
- **2026-06-27 [Quirk]** — The L01 "severity filter" feature is a confidence toggle (`hideLow` boolean), not a severity dropdown. When `hideLow` is true, findings with `confidence < 0.65` are hidden. Severity sort (CRITICAL → WARNING → SUGGESTION) is always applied independently of the toggle. The spec name is misleading — the actual implementation is in `FindingsPanel.tsx` (`hideLow` state) and `constants.ts` (`LOW_CONFIDENCE_THRESHOLD`, `SEVERITY_ORDER`). `client/src/app/repos/[repoId]/pulls/[number]/_components/FindingsPanel/`
- **2026-06-27 [Pattern]** — Popup detail data sourced from already-fetched React Query cache rather than a new endpoint: `FindingsTab` builds `Map<run_id, ReviewRecord>` from the `usePrReviews` result and passes it as `reviewsByRunId` to `RunHistory`; clicking a severity badge filters in-memory. No new route, no extra network call. Apply when popup data is a subset of something already loaded on the page. `client/src/app/repos/[repoId]/pulls/[number]/_components/FindingsTab/FindingsTab.tsx`, `RunHistory/RunHistory.tsx`
- **2026-06-27 [Pattern]** — Lazy-fetch popup for list rows: wrap the data-fetching popup as a child component (`FindingsPopup`) that only mounts when popup state is non-null. The `usePrReviews(prId)` inside fires only on first click, not on page load for every PR row. Contrast with the RunHistory pattern above (cache reuse) — use this when popup data is NOT already in cache. `client/src/app/repos/[repoId]/pulls/_components/PRRow/PRRow.tsx`
- **2026-06-27 [Pattern]** — Severity badge always shows all 3 types when total findings > 0: remove `if (!cnt) return null` guard, render all three with 0-count at `opacity: 0.45` and full severity color. Prevents visual confusion when one run has CRITICAL only and the user expects a stable 3-column layout. `client/src/app/repos/[repoId]/pulls/_components/PRRow/PRRow.tsx`
- **2026-06-27 [Pattern]** — Use `ReactDOM.createPortal(popup, document.body)` for `position: fixed` popups rendered inside grid-row components. CSS `transform` on any ancestor makes `position: fixed` relative to that ancestor, not the viewport — portaling to `document.body` escapes the stacking context unconditionally. `client/src/app/repos/[repoId]/pulls/_components/PRRow/PRRow.tsx`
- **2026-06-27 [Mistake]** — `FindingsPanel` used `findings[0]?.id` as the reset dependency for `activeSeverity`/`focusIdx`. Two different runs can share a first-finding ID (seeded data), silently leaving a stale filter active. Fix: add `runId?: string` prop and use `runId ?? findings[0]?.id` as the dep — stable run-level identity. `client/src/app/repos/[repoId]/pulls/[number]/_components/FindingsPanel/FindingsPanel.tsx`
- **2026-06-27 [Quirk]** — Next.js HMR file watcher on Windows can miss changes made by external tools (Claude Code edit, vim, etc.). Symptom: source file is newer than `.next/static/chunks/…/page.js` but the browser keeps serving the old bundle. Fix: delete the stale chunk file — Next.js recompiles from source on the next page request. `Ctrl+Shift+R` alone does not help if the server never recompiled. `client/.next/static/chunks/`
- **2026-06-27 [Decision]** — Run Review button moved to its own `actions` grid column (index 7, 110 px) between COST and UPDATED. Grid expanded from 8 to 9 columns: `"1fr 132px 92px 60px 120px 118px 80px 110px 78px"`. Button always visible (`kind="secondary"`, dark style), not hover-conditional. UPDATED column always shows the timestamp. `client/src/app/repos/[repoId]/pulls/constants.ts`, `PRRow/PRRow.tsx`
- **2026-06-28 [Mistake]** — `FakeEventSource.emit` helper in SSE tests: if you call `onmessage` and then loop `this.listeners[type]` for the same `"message"` type you double-fire, producing 2× as many events as expected. Fix: branch strictly — `"message"` type fires only `onmessage`; named types (`"info"`, `"tool"`, etc.) fire only `addEventListener` listeners. The real browser `EventSource` does the same separation. `client/src/lib/hooks/useRunEvents.test.ts`
- **2026-06-28 [Pattern]** — Inline-style vendor components that need hover without per-render state: inject a `<style>` block once at module load guarded by `document.getElementById(id)`, use a `data-*` attribute as the CSS selector. Zero React re-renders on hover vs. `useState(false)` + `onMouseEnter/Leave` which cause 2 extra renders per hover cycle. Applied in `Button.tsx`: `data-ddbtn={kind}` + `[data-ddbtn="primary"]:hover{…}`. `client/src/vendor/ui/primitives/Button.tsx`
- **2026-06-28 [Quirk]** — The PR list `tableCard` container has `overflow: hidden` (for border-radius clipping); this silently clips any `position: absolute` descendant — including the `Dropdown` panel — regardless of `zIndex`. Symptom: dropdown appears cut off or invisible. Fix applied to the shared `Dropdown` primitive: panel now uses `createPortal(panel, document.body)` at `position: fixed` with coordinates from `getBoundingClientRect()`, escaping all ancestor overflow constraints. `zIndex` raised to 1100 to sit above other overlays. `client/src/vendor/ui/kit/Dropdown.tsx`, `client/src/app/repos/[repoId]/pulls/styles.ts:116`
- **2026-06-28 [Quirk]** — Both i18n namespaces needed for the Skills feature are pre-populated. `client/messages/en/skills.json` has complete strings for the skills page, import drawer (file/URL/community tabs), list items, and the skill preview/editor panel. `client/messages/en/agents.json` has `skills.*` keys for the Skills tab inside the Agent Editor (`title`, `enabledCount`, `filterPlaceholder`, `orderHint`). Check these files before adding new translation keys — the majority of skill UI strings already exist. `client/messages/en/skills.json`, `client/messages/en/agents.json`
- **2026-06-29 [Pattern]** — Portaled `position:fixed` dropdown panels still clip at the viewport bottom when the trigger is near the bottom of the page. Fix: at toggle time compute `spaceBelow = window.innerHeight - rect.bottom` and `spaceAbove = rect.top`; when `spaceBelow < spaceAbove || spaceBelow < 160`, open upward using CSS `bottom: window.innerHeight - rect.top + 6` instead of `top`. Always add `maxHeight` clamped to the available space and `overflowY: auto` as a safety fallback. `client/src/vendor/ui/kit/Dropdown.tsx`
- **2026-06-29 [Mistake]** — `RunReviewDropdown` in `PRRow` must use `onRunsStarted` (fires after API success) for `router.push()`, NOT `onRunStart` (fires before the HTTP POST). Using `onRunStart` navigates while `mutateAsync` is still pending: React starts unmounting `PRRow` before the request fires, and TanStack Query cancels the in-flight mutation — the run never starts. `client/src/app/repos/[repoId]/pulls/_components/PRRow/PRRow.tsx`
- **2026-06-30 [Quirk]** — `useSearchParams()` from `next/navigation` returns `null` in jsdom test environments (no Next.js router context). Components calling `params.get(...)` unconditionally will throw `Cannot read properties of null (reading 'get')` in all tests that render them without a Next.js router wrapper. Fix: use optional chaining (`params?.get(...)`) in the component. The type already includes `| null` so TypeScript accepts the guard without a cast. `client/src/app/repos/[repoId]/pulls/[number]/_components/FindingCard/FindingCard.tsx`
- **2026-06-30 [Quirk]** — Client-side `@devdigest/shared` is a vendored copy at `client/src/vendor/shared/` that diverges silently from the server copy. When the server adds new fields to a shared type (e.g., `SmartDiffFile.findingsCount`, `.findings`), the client copy must be updated in the same change set or the client typecheck will see an older schema. Always diff `server/src/vendor/shared/contracts/` vs `client/src/vendor/shared/contracts/` when new fields appear in plan pre-researched facts. `client/src/vendor/shared/contracts/brief.ts`
- **2026-07-02 [Quirk]** — `OverviewTab.tsx` contains an inline duplicate of `BlastRadiusCard` + `SymbolImpact` (lines ~20–178), and it is the one actually rendered; the standalone `BlastRadiusCard.tsx` / `SymbolImpact.tsx` files in the same folder are never imported. Editing the standalone files has no visible effect. Consolidate to the standalone files (planned in `docs/plans/2026-07-02-blast-radius.md` Step 4) or edit the inline copy until then. `client/src/app/repos/[repoId]/pulls/[number]/_components/OverviewTab/OverviewTab.tsx:20` → Resolved 2026-07-02: inline duplicates deleted, standalone files are now the single implementation.
- **2026-07-02 [Quirk]** — `s.cardGrid` in `OverviewTab/styles.ts` had explicit `alignItems: "start"` which prevented CSS grid's default equal-height stretching of cells. Removing it (letting the grid default `stretch` apply) is what makes both Intent and Blast Radius cards share the same height when one grows. Never add `alignItems: "start"` to a two-column grid where you want equal-height cards. `client/src/app/repos/[repoId]/pulls/[number]/_components/OverviewTab/styles.ts`
- **2026-07-02 [Mistake]** — RTL `getByText("POST")` fails when the full text content of the span is "POST /checkout" (two JSX expressions `{method} {path}` create a single text node). You must use `getByText("POST /checkout")` or a regex. Never query just the method substring when the component renders `{method} {path}` together. `client/src/app/repos/[repoId]/pulls/[number]/_components/OverviewTab/BlastRadiusCard.test.tsx`
- **2026-07-02 [Pattern]** — `vi.mock("next/dynamic")` in a `.test.tsx` file: return a factory that ignores the loader and returns a stub component. With Vitest's automatic JSX runtime (via `@vitejs/plugin-react`), JSX in the mock factory compiles correctly even though `vi.mock` is hoisted. Use `data-testid` on the stub to assert which dynamic component is rendered without loading the actual bundle. Applied to test the Tree/Graph toggle without loading React Flow. `client/src/app/repos/[repoId]/pulls/[number]/_components/OverviewTab/BlastRadiusCard.test.tsx`
- **2026-07-02 [Quirk]** — `@xyflow/react` v12 (React Flow): use `proOptions={{ hideAttribution: true }}` to suppress the "Powered by React Flow" watermark. The v11 equivalent was `proOptions={{ account: 'paid-pro' }}`. CSS must be imported as `@xyflow/react/dist/style.css` (not the old `reactflow/dist/style.css`). Direct jsdom tests are not practical even with ResizeObserver polyfilled — React Flow's layout engine requires real canvas dimensions. Mock `next/dynamic` at the card level instead. `client/src/app/repos/[repoId]/pulls/[number]/_components/OverviewTab/BlastGraph.tsx`
- **2026-07-02 [Decision]** — BlastGraph caller nodes (column 2) use `BlastCaller.name` (e.g. `publicRouter`), NOT `file:line` paths. Deduplication is by caller name. Click still opens GitHub blob URL using the first occurrence's `file:line`. The Tree view (SymbolImpact) retains file:line paths for detailed inspection. Matching this to the design screenshot cost one refactor cycle — check design screenshots for the label convention before implementing graph nodes. `client/src/app/repos/[repoId]/pulls/[number]/_components/OverviewTab/BlastGraph.tsx`
- **2026-07-02 [Mistake]** — `@xyflow/react` custom node types MUST render `<Handle type="target">` and `<Handle type="source">` components — React Flow SILENTLY drops every edge whose endpoint node has no matching handle (no error in the UI, graph renders nodes only). Symptom: all edges missing despite correct `source`/`target` ids. Fix: render invisible handles (`opacity: 0, pointerEvents: 'none'`, `isConnectable={false}`) inside the custom node. Also: edge `type: "smoothstep"` renders orthogonal stepped lines; use `type: "default"` for the smooth bezier curves shown in the design. `client/src/app/repos/[repoId]/pulls/[number]/_components/OverviewTab/BlastGraph.tsx:154`
- **2026-07-03 [Decision]** — The Project Context page (`repos/[repoId]/context/page.tsx`) is a true async Server Component (`export default async function`, `await params`) that renders a `"use client"` view with `repoId` as a plain string prop — the FIRST page in the codebase to actually follow the documented Next 15 `await params` convention. Every other `repos/[repoId]/...` and `agents/[id]` page is `"use client"` at the page level and reads params via `useParams()` instead (see `pulls/[number]/page.tsx`, `agents/[id]/page.tsx`, `conventions/page.tsx`). Do not "fix" the Context page back to the `useParams()` pattern to match those — it was deliberately built the other way per plan `docs/plans/2026-07-02-project-context-folder.md` Step 7. `client/src/app/repos/[repoId]/context/page.tsx`
- **2026-07-03 [Mistake]** — `docs/plans/2026-07-02-project-context-folder.md` Step 1 said "leave `vendor/ui/nav.ts` untouched — Step 7 populates the page these keys point at," but Step 7's own file list never included `nav.ts`, so no step ever added a `context` entry to `NAV`. The page shipped but was unreachable from the sidebar (no nav item, though `helpers.ts:30`'s `activeKeyFor` already matched `/context`). When a plan step defers adding a nav/route/UI entry to "a later step," grep that later step's own `Owned paths` list to confirm the deferred file is actually named there — do not trust the cross-reference prose alone. `client/src/vendor/ui/nav.ts`
- **2026-07-03 [Quirk]** — `navigator.clipboard` in jsdom/Vitest is inconsistently present depending on test file isolation and jsdom version: `vi.stubGlobal("navigator", { ...navigator, clipboard: {...} })` silently fails to override `writeText` (jsdom's real `Clipboard` EventTarget wins), and whether `navigator.clipboard` already exists at test start also varies between an isolated single-test run (`-t` filter) and a full-file run. Robust pattern: `if (!navigator.clipboard) Object.defineProperty(navigator, "clipboard", { value: { writeText: fn }, configurable: true }); else vi.spyOn(navigator.clipboard, "writeText").mockImplementation(fn);` inside a `beforeEach`. `client/src/app/repos/[repoId]/onboarding-tour/_components/OnboardingTourView/OnboardingTourView.test.tsx`
- **2026-07-03 [Pattern]** — For a client feature step that proceeds in parallel with its server counterpart (mocked-API convention), mock the feature's own hooks module (`vi.mock("@/lib/hooks/<feature>", ...)`) in the container test rather than mocking `fetch` — this matches the established `ContextDocsView.test.tsx` pattern (mock `useActiveRepo`/`useRepoNotFound`/`@/components/app-shell`/`@/components/repo-not-found` plus the feature's hooks module) and keeps the test decoupled from the real API response shape until the server module lands. `client/src/app/repos/[repoId]/onboarding-tour/_components/OnboardingTourView/OnboardingTourView.test.tsx`
- **2026-07-03 [Quirk]** — `lucide-react`'s `Icon` wrapper (v0.469.0) forwards the `color` prop to the underlying `<svg>` as the `stroke` attribute, NOT a `color` attribute (`Icon.js`: `stroke: color`). RTL assertions on icon color must query `svg.getAttribute("stroke")`, never `getAttribute("color")` — the latter is always `null`. Also: string-concatenating an alpha suffix onto a CSS custom property token (e.g. `` `${"var(--crit)"}1f` ``) produces invalid CSS and silently no-ops; only works for raw hex tokens. When building a tinted icon chip from a mixed hex/CSS-var color set, branch explicitly per token and reuse the paired `*-bg` custom property (e.g. `--crit-bg`, `--warn-bg` already defined in `vendor/ui/styles.css`) instead of string concatenation. `client/src/app/repos/[repoId]/pulls/[number]/_components/OverviewTab/IntentCard.tsx`, `client/src/vendor/ui/styles.css`
- **2026-07-03 [Mistake]** — When converting a fully-clickable card (whole card is one `<button>`) into an accordion card that ALSO needs a nested clickable `path:line` link inside it (with its own `onClick` + `stopPropagation`), a `<button>` cannot contain another `<button>`/`<a>` — invalid HTML, and browsers silently drop/misrender the nested element's click handling. Fix: change the outer card to `<div role="button" tabIndex={0} onClick={...} onKeyDown={(e) => { if (e.key === "Enter" || e.key === " ") { e.preventDefault(); onToggle(); } }} aria-expanded={...}>` (precedent already existed in `ReviewRunAccordion.tsx:85-91`) so the inner `<button>` for the link stays valid and independently clickable via `stopPropagation`. `client/src/app/repos/[repoId]/pulls/[number]/_components/OverviewTab/IntentCard.tsx`
- **2026-07-03 [Pattern]** — When a component has an early-return "gated" state (e.g. `OverviewTab` rendering only an empty-state block until `brief.llm` exists) and the full-layout branch needs many non-optional reads off that same nullable object, guard the early return directly on the nullable expression (`if (!brief?.llm) { return <Empty/> }`) rather than deriving a separate `const hasBrief = !!brief?.llm` boolean. TypeScript's control-flow narrowing only follows the exact expression used in the `if` — a derived boolean does NOT narrow `brief.llm` back to non-null afterward, forcing awkward `!` assertions or repeated `?.` all the way down. `client/src/app/repos/[repoId]/pulls/[number]/_components/OverviewTab/OverviewTab.tsx`
- **2026-07-04 [Quirk]** — `@devdigest/ui`'s `Icon` registry (`client/src/vendor/ui/icons.tsx`) only exports `Icon.Trash` from lucide-react — there is no `Trash2` (nor a separate `X` alias beyond the existing `Icon.X`). Reach for `Icon.Trash` directly for any destructive/clear action icon instead of assuming the common `Trash2` variant exists; check `icons.tsx`'s import list before adding a new icon usage. `client/src/vendor/ui/icons.tsx`
- **2026-07-04 [Mistake]** — Server-side smart-diff composition (`GET /pulls/:id/smart-diff` or its equivalent) can emit the same `file.path` more than once within a single `SmartDiffGroup.files` array for large PRs. This produces a React "two children with the same key" warning at the `group.files.map(...)` call site AND, more seriously, duplicate DOM ids (`diff-file-${path}`, `diff-line-${path}-${line}`), so `document.getElementById` for internal diff navigation can resolve to the wrong (first-matching) node non-deterministically. Client-side fix: dedupe `group.files` by `path` (keep first occurrence) before mapping in `GroupView` — do NOT just suffix the React key to silence the warning, that still double-renders the file. The root cause is server-side and remains open as a follow-up. `client/src/app/repos/[repoId]/pulls/[number]/_components/DiffTab/SmartDiffViewer.tsx`
- **2026-07-05 [Mistake]** — Adding a new TanStack Query hook call inside a leaf component (`FindingCard` calling `useCreateEvalCaseFromFinding`) breaks EVERY sibling/parent test that renders that leaf component without a `QueryClientProvider` — even tests that only exercise unrelated props (`FindingsPanel.test.tsx` failed with "No QueryClient set" despite never touching the new eval feature). Grep for other `*.test.tsx` files that render the component transitively (parent component tests, not just the component's own test file) and add the same `vi.mock("@/lib/hooks/<feature>", ...)` there too — one new hook call can require updating N unrelated test files up the render tree. `client/src/app/repos/[repoId]/pulls/[number]/_components/FindingsPanel/FindingsPanel.test.tsx`
- **2026-07-05 [Pattern]** — `ReviewRunAccordion` already holds the full `review: ReviewRecord` object (including `agent_id`) for the run it renders — when a descendant several levels down (`FindingsPanel` → `FindingCard`) needs the review's `agentId`, thread it starting from `ReviewRunAccordion` (which already has it in scope), not from the page/route level. Do not assume a new prop needs page.tsx → ... → leaf full-chain threading; check the nearest ancestor that already holds the parent domain object first. `client/src/app/repos/[repoId]/pulls/[number]/_components/ReviewRunAccordion/ReviewRunAccordion.tsx`
- **2026-07-05 [Pattern]** — `@devdigest/ui` already vendors a Recharts-backed `LineChart` (`charts/LineChart.tsx`, exported from the barrel) and `recharts` is an existing `client/package.json` dependency — before hand-rolling an SVG chart for a new trend/metrics view, check `client/src/vendor/ui/charts/` first. A plan step that says "if none exists, hand-roll a minimal SVG chart" may be working from stale research; grep the barrel (`vendor/ui/index.ts` → `./charts`) before assuming no primitive exists. `client/src/vendor/ui/charts/LineChart.tsx`
- **2026-07-05 [Quirk]** — Co-located test files importing `messages/en/<ns>.json` via a relative path must count directory levels exactly from the test file's own folder, not copy the depth used by a sibling test at a different nesting level. `ContextTab.test.tsx` (at `AgentEditor/_components/ContextTab/`) uses 8 `../`; a new tab's container test one level shallower needs 8 as well when nested one extra `_components/<Feature>/` layer deeper (e.g. `EvalsTab/_components/CaseEditor/CaseEditor.test.tsx` needs 10 `../`). Compute with `path.relative(path.dirname(testFile), 'messages/en/x.json')` rather than eyeballing an existing sibling's prefix — Vite fails fast with a clear "Failed to resolve import" error if it's off by one, which is the fastest way to catch it. `client/src/app/agents/[id]/_components/AgentEditor/_components/EvalsTab/EvalsTab.test.tsx` (2026-07-07: recurred a third time for a component nested two `_components/` layers under `SkillDetail` — `EvalsTab/_components/SkillEvalBatchHistory/SkillEvalBatchHistory.test.tsx` needed 9 `../`, not 8; a bash `ls` probe of the candidate path is the fastest way to confirm the count before running the suite.)
- **2026-07-05 [Quirk]** — `screen.getByDisplayValue(multilineString)` reliably fails to match a controlled `<textarea>` in jsdom/RTL when the expected string contains embedded `\n` (e.g. a pre-filled unified diff fragment) — RTL's own multi-line-normalization heuristics do not consistently line up with the textarea's raw `.value`, even though the DOM node visibly contains the exact text (confirmed via the pretty-printed DOM dump in the failure output). Fix: query the specific element by its stable attribute instead (`screen.getByPlaceholderText("...")`) and assert with `.toHaveValue(expectedString)` — this compares the raw `.value` property directly and is immune to the normalization mismatch. Applies to any RTL assertion on a pre-filled multi-line `Textarea`/`<textarea>`, not just diff fragments. `client/src/app/agents/[id]/_components/AgentEditor/_components/EvalsTab/_components/CaseEditor/CaseEditor.test.tsx`
- **2026-07-06 [Mistake]** — Wrapping the custom `SelectInput` (any widget whose open state renders `<button>` options) in a raw `<label>` makes a single click silently self-select `option[0]` and never visibly open the list. React flushes the open-state re-render **synchronously** for discrete events (click), so the dropdown `<button>`s are already in the DOM when the same click bubbles up to the `<label>`, which then fires a synthetic click on its first labelable descendant (the first option button) → `onChange(option[0])` + close. Symptom: clicking the picker flips the value to the first option and "won't open". Fix: use a plain `<div>` + `<span>` caption for custom widgets — `<label>` is only for real form controls (`<input>`/`<textarea>`). All other `SelectInput` call sites wrap it in `<FormField>`, which does not forward the click, so only the one raw-`<label>` site was affected. Regression-guarded in `CaseEditor.test.tsx`. `client/src/app/agents/[id]/_components/AgentEditor/_components/EvalsTab/_components/CaseEditor/CaseEditor.tsx`, `client/src/vendor/ui/kit/SelectInput.tsx`
- **2026-07-06 [Quirk]** — `useAgents()` (`GET /agents`) has no explicit sort: the shared `Agent` contract (`vendor/shared/contracts/knowledge.ts`) has no `created_at` field at all, and `AgentsRepository.list()` (`server/src/modules/agents/repository.ts:54`) has no `.orderBy(...)` — rows come back in whatever order Postgres/Drizzle happens to return them (implementation-defined, not a documented insertion-order guarantee). A plan step that says "ordered by `created_at`, confirm `useAgents()`'s existing sort" is describing a sort that does not exist; treat "first agent in the list" as simply `list[0]` of whatever `useAgents()` returns, and do not add a client-side sort unless the plan explicitly asks for one. `client/src/lib/hooks/agents.ts`, `server/src/modules/agents/repository.ts`
- **2026-07-06 [Mistake]** — A multi-step plan can leave a route half-migrated: `server/src/modules/skills/routes.ts`'s `POST /skills/:id/evals` still calls the OLD `SkillsService.createEvalCase` (no AC-2/AC-3 validation, no `input_meta.source: 'manual'` default), while the NEW `SkillEvalService.createCaseManual` (which DOES validate and default correctly) exists in `eval-service.ts` but was never wired to any route — only `PATCH` (`updateCase`) got a route in `eval-routes.ts`. Always grep the service file for unwired public methods (`grep -n "async " eval-service.ts` vs. every route file's call sites) before assuming "the route exists because the service method exists" — a plan can pin a service method's behavior in one step and forget the route in another. Client-side workaround applied here: `useCreateSkillEval` posts the OLDER wire shape (`{name, input_diff, expected_output, notes}`, with `practices/grounding/threshold` packed into `expected_output`) to the currently-live route, since posting the newer `SkillEvalCaseCreateInput` shape directly would silently fail Zod's `CreateEvalCaseBody` (extra keys stripped, `input_diff` missing → 400). `server/src/modules/skills/routes.ts:190-199`, `server/src/modules/skills/eval-service.ts:58`, `client/src/lib/hooks/skills.ts`
- **2026-07-06 [Quirk]** — lucide-react's inline `color` style prop never appears on the `stroke` DOM attribute for RTL assertions (confirmed again independently — see the existing 2026-07-03 IntentCard entry); distinguishing two same-shaped-but-differently-colored status icons in a test must compare `element.style.color` (or the rendered `class` for a genuinely different icon), never `getAttribute("stroke")`, which is always `"currentColor"` regardless of the `color` prop. `client/src/app/skills/_components/SkillDetail/EvalsTab.test.tsx`
- **2026-07-07 [Quirk]** — `@devdigest/ui`'s `Sparkline` primitive divides by `data.length - 1` to space points along the x-axis; when `data` has exactly one point this is `0 / 0 = NaN`, producing a React "Received NaN for the `cx` attribute" console warning (not a test failure, but noisy and easy to mistake for a bug in the NEW consumer rather than the primitive). A single-point sparkline is a legitimate real-world case (agent's first eval batch). Workaround in a consumer's own test fixtures: use ≥2 points. The primitive itself was left unfixed as out-of-scope (`client/src/vendor/ui/` is not owned by feature-scoped plan steps) — a future step that touches `vendor/ui/charts/Sparkline.tsx` should guard `data.length <= 1` and render a flat/dot fallback. `client/src/vendor/ui/charts/Sparkline.tsx`, `client/src/app/evals/_components/AgentCard/AgentCard.tsx`
- **2026-07-07 [Mistake]** — Adding `useRouter()` from `next/navigation` to a leaf component (`EvalMetrics`, to activate its "View full dashboard" link) breaks every existing test of its PARENT container (`EvalsTab.test.tsx`, 15/16 tests) with `Error: invariant expected app router to be mounted` — the container's own test file had never needed a router mock before, since none of its previously-rendered children called `useRouter`. Same class of failure as the 2026-07-05 "new hook call breaks sibling tests" entry, but for router APIs instead of TanStack Query hooks. Fix: add `vi.mock("next/navigation", () => ({ useRouter: () => ({ push: vi.fn() }) }))` to the parent's test file too, not just the leaf's own new test file — grep every `*.test.tsx` that renders the leaf transitively before declaring the task done. `client/src/app/agents/[id]/_components/AgentEditor/_components/EvalsTab/EvalsTab.test.tsx`
- **2026-07-07 [Pattern]** — In a multi-agent wave build (Agent Eval Dashboard plan), a "later wave" step can land its file edits before an "earlier wave" implementer instance actually starts — e.g. Step 14's `agentId?: string` addition to `EvalMetrics.tsx` and its `useRouter` wiring were already present when the Step 12 instance began, discoverable only via `git status --porcelain` (untracked/modified files beyond the current task's own scope), not from the plan document itself. Before composing an existing shared component in a parallel-wave task, run `git status --porcelain` first and re-read that component's CURRENT file rather than trusting the plan's prose description of its props — the plan can describe a component's state as of when it was written, not as of when your instance actually runs. `client/src/app/agents/[id]/_components/AgentEditor/_components/EvalsTab/_components/EvalMetrics/EvalMetrics.tsx`
- **2026-07-07 [Mistake]** — A word-level LCS diff that tokenizes whitespace as ordinary content tokens (`split(/(\s+)/)`) will spuriously fragment an UNCHANGED word into a `removed`+`added` pair when only the whitespace AROUND it differs between the two strings (e.g. `"one  two three"` vs `"one two  three"` — the LCS optimizer can match an unrelated space run instead of the one adjacent to "two", splitting "two" itself out of the equal run). Root cause: two whitespace runs from different parts of the text look "equal" as bare strings even though they aren't positionally related. Fix: the LCS comparator must treat two whitespace tokens as matchable ONLY when the token immediately preceding each of them (if any) is the same word — i.e. whitespace can only be "equal" as a trailing attachment to an already-equal-matched word, never independently. ~~`client/src/app/evals/_components/CompareModal/diffWords.ts`~~ (path superseded 2026-07-07 — moved to `client/src/components/eval/CompareModal/diffWords.ts`)
- **2026-07-07 [Decision]** — For the Compare-modal Promote gate (AC-22), do NOT reuse `EvalBatchCompareResult.prompt_diff_available` to disable the Promote button — that flag is `a.system_prompt_snapshot != null && b.system_prompt_snapshot != null` (BOTH sides), so it's `false` even when only the OLDER batch lacks a snapshot but the NEWER one (the one actually being promoted) has one. Gating Promote on `prompt_diff_available` would over-disable a legitimate promote. Instead, independently determine which side is chronologically newer (compare `ran_at`, never assume the `b` query param is newer than `a` — the server's `compareBatches` computes `deltas.recall = b.recall - a.recall` positionally, not chronologically) and check ONLY that batch's own `system_prompt_snapshot`. ~~`client/src/app/evals/_components/CompareModal/CompareModal.tsx`~~ (path superseded 2026-07-07 — moved to `client/src/components/eval/CompareModal/CompareModal.tsx`), `server/src/modules/eval/service.ts:387`
- **2026-07-07 [Quirk]** — `git mv` silently fails with `fatal: source directory is empty` when the source path is an UNTRACKED directory (new files never `git add`ed) — git's index has no entry for it, so `git mv` sees nothing to move even though the filesystem directory is non-empty. This hit `evals/_components/CompareModal/` (added by an uncommitted multi-agent plan run, never staged) while every other moved folder in the same refactor was already tracked and `git mv`'d cleanly. Fix: check `git status --porcelain -- <path>` first — if it shows `??`, use a plain filesystem move (`Move-Item` on Windows / `mv` on POSIX) instead of `git mv`; the new location will show as untracked (`??`) same as before, which is expected and fine. `client/src/components/eval/CompareModal/` (moved from `client/src/app/evals/_components/CompareModal/`)
- **2026-07-07 [Pattern]** — When promoting shared components to a neutral `client/src/components/<name>/` location (breaking a two-way page-tree coupling), derive a magic UI constant (e.g. a table row-height px used to size a "N visible rows + scroll" viewport) FROM the shared style tokens it's visually built on (`td` padding + an approximate text line-height) via a small exported named constant in the shared `styles.ts`, rather than leaving each consumer with its own hand-tuned literal. Keeps the "N rows visible" contract from silently drifting from the real rendered row height across independent call sites. `client/src/components/eval/styles.ts` (`TABLE_ROW_HEIGHT_PX`), `client/src/app/evals/_components/RecentRunsFeed/RecentRunsFeed.tsx`
- **2026-07-07 [Pattern]** — The Eval Dashboard `GET /evals/overview` payload (`EvalAgentSummary`) carries a per-agent `latest_batch` but NO pass/total counts, while `GET /evals/recent` (`EvalRecentBatchRow`) DOES compute `pass_count`/`total_count` per batch. To show "N/M pass" on the landing agent cards without a server/contract change, join them client-side by batch id: build `Map<batch.id, {pass,total}>` from the recent feed in `EvalsLandingView` and pass the entry for `summary.latest_batch.id` into `AgentCard` (falls back to `case_count` when the latest batch is older than the recent feed's 25-row cap). Same class of cache-reuse join as the FindingsTab pattern (2026-06-27). `client/src/app/evals/_components/EvalsLandingView/EvalsLandingView.tsx`, `client/src/app/evals/_components/AgentCard/AgentCard.tsx`
- **2026-07-07 [Mistake]** — A nav entry rendered its sidebar label fine but crashed the COMMAND PALETTE with `MISSING_MESSAGE: Could not resolve 'shell.nav.evals'`. Two different label sources: the sidebar (`vendor/ui/shell/NavItem.tsx`) renders the hardcoded `NavItemDef.label` string directly, but `useShellCommands.ts` builds each palette command via `t(\`nav.${it.key}\`)` — so EVERY `NAV[].items[].key` MUST have a matching `shell.nav.<key>` message or the palette throws on mount (an app-wide crash, since AppShell mounts the palette on every page). Here the NAV key was `evals` (nav.ts) but `messages/en/shell.json` had `nav.eval` (singular) → dead key + missing `evals`. When adding/renaming a nav item, grep `messages/en/shell.json`'s `nav` block for the EXACT key; the sidebar rendering will look fine and hide the mismatch until the palette (⌘K, or its build-on-mount) runs. `client/src/vendor/ui/nav.ts`, `client/src/components/app-shell/hooks/useShellCommands.ts`, `client/messages/en/shell.json`
- **2026-07-07 [Quirk]** — The vendored `LineChart` (`vendor/ui/charts/LineChart.tsx`) wraps its `ResponsiveContainer` in `<div style={{ width:"100%", maxWidth: w }}>` with `w=620` default — so no matter how wide the parent block is, the plotted lines are left-aligned and stop at ~620px with dead space on the right (symptom: "chart doesn't fill the card"). It is NOT a Recharts responsiveness bug — it's the explicit `maxWidth` cap. Added a `fill?: boolean` prop that drops the cap (`maxWidth: fill ? undefined : w`); `TrendChart` forwards it as `fillWidth`, and both the Eval-Dashboard detail trend, the agent Evals-tab trend, and the skill `SkillTrendChart` now pass it. Default stays capped so other consumers are unchanged. Same left-aligned-at-fallback-width class of bug as the `Sparkline fill` measurement issue (a nested-flex parent left it stuck at its fallback width — `fill` needs a plain-block parent so `width:100%` resolves). `client/src/vendor/ui/charts/LineChart.tsx`, `client/src/components/eval/TrendChart/TrendChart.tsx`
- **2026-07-07 [Quirk]** — `eval_batches.system_prompt_snapshot` is populated ONLY for batches created AFTER migration 0021 shipped; every older batch (and every calibration batch) reads `null`. Consequence for the Compare modal: `prompt_diff_available` (requires BOTH sides present) is false for almost every real comparison right now, because only each agent's single latest full batch carries a snapshot — so a fresh agent testing "compare two runs" sees no word-diff and may think the diff feature is broken. It isn't. `CompareModal` handles the cases explicitly off `result.a/b.system_prompt_snapshot` (both present+diff → word diff; both+identical → "unchanged" + verbatim; newer present/older null → newer verbatim + note; newer null → "no stored prompt", Promote disabled) — never off `prompt_diff_available` alone. A true two-sided diff only appears once TWO post-0021 full batches exist for the same agent. Verify prompt availability with `select ran_at, (system_prompt_snapshot is not null) from eval_batches` before assuming the UI is wrong. `client/src/components/eval/CompareModal/CompareModal.tsx`
- **2026-07-09 [Quirk]** — A complete, detailed i18n spec for a "CI Runs" screen + Export-to-CI/Publish wizard exists in `client/messages/en/ci.json` (run history strings, wizard steps, publish dialog, page crumb) and a matching Settings→Integrations section exists in `settings.json`, but ZERO components consume either namespace (confirmed via grep for `useTranslations("ci")` / `t("ciTab...")` / `t("exportWizard...")` — no hits anywhere in `src/`). Do not assume a screen exists just because its i18n strings do. Related dead scaffolding discovered alongside it: `app-shell/helpers.ts:38` already branches on `pathname.startsWith("/ci-runs")` for a route that doesn't exist, the Agent Editor's `TABS` array has no "CI" entry despite `agents.json`'s `editor.tabs.ci` label, `SETTINGS_SECTIONS` has no "integrations" entry despite the written copy, and `vendor/ui/ExportWizardSteps.tsx` (a step-indicator component matching `ci.json`'s exact wizard steps) is rendered only inside an internal `Showcase.tsx`, unreachable from any real route. The only real, live UI touching this feature at all is the "CI gate" (`ci_fail_on`) dropdown on the Agent Config tab. Server-side counterpart (why none of this is wired) is tracked in `agent-runner/insights.md`'s 2026-07-09 Open Question entry. `client/messages/en/ci.json`, `client/src/components/app-shell/helpers.ts:38`
- **2026-07-10 [Quirk]** — Two unrelated i18n files both use the top-level key `"runs"` in `client/messages/en/`: `ci.json`'s `runs.*` (the CI Runs page — `runs.title`, `runs.table.*`, `runs.status.*`) and the entirely separate `runs.json` (Multi-Agent Review / `RunTraceDrawer` feature from a sibling lesson — `runs.json`'s own top-level keys like `stat.duration: "DURATION"`, `page.title: "Multi-Agent Review"`). They never collide at runtime (`useTranslations("ci")` vs `useTranslations("runs")` load different files entirely), but a plain-text grep for a key name (e.g. "duration") across `messages/en/` can surface the wrong file's match. Always check which `useTranslations(namespace)` call is in scope before trusting a grep hit inside `messages/en/`. `client/messages/en/ci.json`, `client/messages/en/runs.json`
- **2026-07-10 [Quirk]** — jsdom has no `URL.createObjectURL`/`revokeObjectURL` implementation, so a component that builds a client-side zip (`jszip`) and triggers a `<a download>` on a mutation's `onSuccess` will throw in any test that actually lets that callback run. When a test only needs to assert the mutation's call *arguments* (not the resulting download), mock the mutation's `mutate` to invoke only `onSettled` — never `onSuccess` — which keeps the real `jszip`/`Blob`/`URL.createObjectURL` code path from ever executing in jsdom, no polyfill needed. `client/src/app/agents/[id]/_components/AgentEditor/_components/CITab/_components/ExportWizard/ExportWizard.test.tsx`
- **2026-07-10 [Quirk]** — PowerShell's `Get-ChildItem -Path` (and other path-wildcard cmdlets) parse `[id]`/`[repoId]`/`[number]`-style Next.js dynamic-route folder names as a wildcard character class (matching a single literal `i` or `d`), not as a literal bracketed folder name — it silently returns EMPTY output instead of erroring, which reads as "the directory doesn't exist" rather than "the path syntax was misparsed." This directly undermines the project convention of falling back to PowerShell when `Glob` times out, for exactly the path shape (`agents/[id]/...`, `repos/[repoId]/...`, `pulls/[number]/...`) that fallback is most likely to be used on. Fix: always pass `-LiteralPath` instead of `-Path` for any command whose path contains square brackets. `client/src/app/agents/[id]/`, `client/src/app/repos/[repoId]/`
- **2026-07-10 [Quirk]** — The Claude Code `Glob` tool itself (not just PowerShell) also mis-parses `[id]`/`[repoId]`-style bracketed folder names as a character class and silently returns zero matches — same failure shape as the PowerShell entry above, but in the tool that entry's own "fall back to PowerShell" advice assumed was unaffected. Fix: escape the brackets in the glob pattern itself (`agents/\[id\]/_components/...`), or use `find`/`Read`/`Grep` instead. `client/src/app/agents/[id]/`
- **2026-07-10 [Quirk]** — `lucide-react` v0.469.0 aliases some of this project's `Icon` registry names (`client/src/vendor/ui/icons.tsx`) to a differently-named internal component: `CheckCircle` re-exports `CircleCheckBig` and `XCircle` re-exports `CircleX` (confirmed via `node_modules/lucide-react/dist/esm/icons/check-circle.js` and `.../createLucideIcon.js`, which derives the rendered `class` from the INTERNAL component name, not the export alias). The rendered `<svg>` gets `class="lucide lucide-circle-check-big"` / `"lucide lucide-circle-x"` — NOT `lucide-check-circle`/`lucide-x-circle` as a naive kebab-case of the registry key would predict. Most other icons (`RefreshCw`, `Slash`, `GitBranch`, `Check`, etc.) are NOT aliased and kebab-case correctly. A test hardcoding an expected icon class string can silently target the wrong string for an aliased icon — see the Pattern entry above for a version-proof alternative. `client/src/vendor/ui/icons.tsx`

## Open Questions
<!-- Unresolved. Convert to an entry in the appropriate section when answered. -->

---
Last updated: 2026-07-10 · Entries: 76
