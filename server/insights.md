# insights.md — server

> Append-only. Add new entries at the bottom of the correct section.
> Discovery bar: "Would a fresh agent save ≥10 minutes from reading this?" If not, skip.
> Format: `**YYYY-MM-DD [Category]** — actionable sentence. \`file:line\``
> See `.claude/skills/engineering-insights/` for full criteria and format rules.

## Patterns
<!-- Reusable approaches that worked in this module. -->
- ~~**2026-06-26 [Pattern]** — `estimateCost(model, tokensIn, tokensOut)` computes USD cost from existing `agent_runs` columns, avoiding a new column and keeping pricing current when the pricing table changes. Before adding a column for a derived numeric value, check whether it can be computed at read time. `server/src/modules/reviews/repository/run.repo.ts:68`~~ (superseded 2026-06-27 — `cost_usd` IS stored per run in `agent_runs` (migration 0010); `estimateCost` is called at write time inside the run executor, not at read time. The PR-list route aggregates stored values via `SUM(cost_usd)` at query time, avoiding a separate pr-total column. `server/src/modules/pulls/routes.ts:132`)
- ~~**2026-06-27 [Pattern]** — Popup detail data sourced from already-fetched React Query cache rather than a new endpoint: `FindingsTab` builds `Map<run_id, ReviewRecord>` from the `usePrReviews` result and passes it to `RunHistory`; clicking a severity badge filters in-memory. No new route, no extra network call. Apply this pattern when popup data is a subset of something already loaded on the page. `client/src/app/.../FindingsTab/FindingsTab.tsx`, `RunHistory/RunHistory.tsx`~~ (superseded 2026-06-27 — this is a client-side React Query pattern; moved to `client/insights.md`)
- **2026-06-28 [Pattern]** — Agent-side skill linking is already registered and functional: `GET/POST /agents/:id/skills`, `AgentsRepository.linkedSkills/linkSkill/setSkills/unlinkSkill`, and `AgentsService.skillLinks/setSkills/linkSkill` all exist and are live. The new `skills` module only needs skill CRUD (create/read/update/delete + version history + import). Do NOT add linking routes to the skills module — they already live in the agents module. `server/src/modules/agents/routes.ts:145`, `server/src/modules/agents/repository.ts:192`
- **2026-07-10 [Pattern]** — Hermetically testing a service whose constructor takes `Container` and lazily constructs MULTIPLE repositories from `container.db` (e.g. `IngestService` building both `InstallationsRepository` AND `RunsRepository`, mirroring `BlastService`'s single-repo `_repo`/`get repo()` lazy pattern but doubled) requires ONE combined fake `Db` satisfying every touched table's exact Drizzle chain shape at once — the private lazy getters can't be swapped individually from a test, unlike `BlastService`'s optional constructor-injected `repo` param. Merge each dependency's OWN test file's fake-db convention (here: `ci-installations-repository.test.ts`'s `select().from().where()` shape for `listTracked` + `ci-runs-repository.test.ts`'s full `insert().values().onConflictDoUpdate().returning()` upsert simulation for `ci_runs`/`settings`) into one `select`/`insert`/`update` dispatcher keyed on table identity (`table === t.xxx`). `server/test/ci-ingest-service.test.ts`
- **2026-07-10 [Pattern]** — A service method wrapped in `withTimeout(p, ms)` (`platform/resilience.ts`) is deterministically testable WITHOUT waiting `ms` of real wall-clock time: `vi.useFakeTimers()`, start the call, attach `expect(promise).rejects.toBeInstanceOf(...)` BEFORE advancing, then `await vi.advanceTimersByTimeAsync(ms)` — `Promise.race`'s losing (never-settling) operand is harmless, and fake timers don't block the DB-fake microtask chain that runs before the operation actually hangs. No prior test in this codebase exercised `withTimeout`/`TimeoutError` this way (confirmed via grep) despite `resilience.ts` being used in `octokit.ts`, `jobs.ts`, and both `repo-intel/pipeline/{incremental,full}.ts`. `server/test/ci-ingest-service.test.ts` (`checkForNewResults` timeout test)
- **2026-07-11 [Pattern]** — Reconciling two branches that each added a Drizzle migration at the SAME `_journal.json` idx (parallel-feature merge: A's `0024_multi_agent_run_linkage` vs B's own idx-24 `0025_ci_run_pr_title`, both shipping a `meta/0024_snapshot.json`) canNOT use the naive "keep one branch's snapshot, rename the other's" recipe — NEITHER pre-existing snapshot contains BOTH table-sets (each was chained off the shared `0022` base, so A's snapshot lacks B's CI tables and vice-versa), so `db:generate` sees the missing tables and wants to emit a migration. Key realization that unlocks the fix: `db:generate` only diffs the current `schema/` against the SINGLE latest (head) snapshot in the journal — it never reads intermediate snapshots — and `db:migrate` ignores snapshots entirely (uses only `.sql` + journal). So the ONLY artifact that must exactly equal the merged schema is the head snapshot; intermediates just need a plausible id/prevId link. Working recipe ("drizzle graft"): (1) write the journal to the target idx sequence and drop in a PLACEHOLDER head snapshot (e.g. one branch's, missing the other's tables) with `prevId` re-pointed to the prior entry's id; (2) `pnpm db:generate` writes a throwaway `NNNN_<slug>.sql` + `meta/NNNN_snapshot.json` whose snapshot is a FULL serialization of the current merged `schema/` (exactly what the head must be) and appends a journal entry; (3) copy that generated snapshot over the head snapshot file, fixing its `id`/`prevId`; (4) delete the throwaway `.sql` + `meta/NNNN_snapshot.json` and remove the appended journal entry; (5) `pnpm db:generate` again → "No schema changes" proves the chain. Then confirm applied-schema == snapshot by running `db:migrate` on a throwaway clean DB (`CREATE DATABASE devdigest_mergetest`; migrate with `DATABASE_URL=...mergetest pnpm db:migrate` — an already-set env var overrides `dotenv`'s `.env`, so the live DB is never touched — then `DROP DATABASE`). Cross-check that each branch's hand-written `.sql` is byte-identical to what drizzle would generate for that delta (it was here: A's `0024...sql` == the grafted diff, FK `ON DELETE set null`) so no drift hides between the `.sql` path and the snapshot path. `server/src/db/migrations/meta/_journal.json`, `server/src/db/migrations/meta/0025_snapshot.json`

## Mistakes
<!-- Failure modes, antipatterns, wrong assumptions. Prioritize this section. -->
- **2025-06-01 [Mistake]** — The server starts without error even when migrations haven't been applied; the first DB query fails with `relation "x" does not exist`, not on startup. Always run `cd server && pnpm db:migrate` before `pnpm dev` on a fresh DB.
- **2026-06-27 [Mistake]** — One-off debugging commands (hardcoded psql connection strings, UUID-bearing curl calls, temp script invocations) accumulated in `.claude/settings.json` over multiple sessions and were committed, creating a credentials and hygiene liability. The canonical allowed-set is the 12 entries currently in the file; any session-specific addition must be removed before committing. For DB access use `docker exec devdigest-postgres psql -U devdigest -d devdigest`; for API inspection use `/workspaces` → `/repos` → `/pulls` without hardcoding IDs. `.claude/settings.json`
- **2026-06-27 [Mistake]** — `docker exec` wildcards in `.claude/settings.json` expand the prompt-injection blast radius: `docker exec *` allows any container; `docker exec devdigest-postgres *` allows any command in the DB container. Scope to `docker exec devdigest-postgres psql -U devdigest -d devdigest *` — limits execution to psql only in the known dev container. `.claude/settings.json`
- **2026-06-27 [Mistake]** — The catch block in `runOneAgent` called `completeAgentRun` without `costUsd`, so failed/cancelled runs always stored NULL cost even when `reviewPullRequest` had already returned and cost was available (e.g. a DB write failure after the LLM call). Fix: declare `let partialCostUsd: number | null = null` before the try, assign it from `outcome.costUsd` after `reviewPullRequest` returns, and pass it in the catch call. `server/src/modules/reviews/run-executor.ts`
- **2026-06-27 [Mistake]** — Backfill migration SQL must mirror `pricing.ts` exactly — any model in the TypeScript table but absent from the SQL CASE is silently left NULL. When adding a model to `pricing.ts`, also add the WHEN clause to any open backfill or create a new one. `server/src/db/migrations/0011_backfill_cost_usd.sql`
- **2026-06-27 [Mistake]** — `completeAgentRun` used `costUsd: values.costUsd ?? null` unconditionally, so callers omitting `costUsd` overwrote a previously stored real cost with NULL. Fixed with a conditional spread `...(values.costUsd !== undefined ? { costUsd: values.costUsd } : {})`. Apply this pattern to any nullable column that multiple code paths (success + error) can update independently. `server/src/modules/reviews/repository/run.repo.ts`
- **2026-06-27 [Mistake]** — The catch block in `runOneAgent` could double-write `completeAgentRun`: if the success path wrote `status='done'` and then `saveRunTrace` threw, the catch would overwrite with `status='failed'`. Fix: `let runCompleted = false` before try; set `true` after the success `completeAgentRun`; wrap the catch-path call in `if (!runCompleted)`. Apply this guard any time a try/catch has completion writes in both branches. `server/src/modules/reviews/run-executor.ts`
- **2026-06-27 [Mistake]** — The catch block in `runOneAgent` passed `tokensIn: 0, tokensOut: 0, findingsCount: 0, grounding: '0/0 passed'` to `completeAgentRun` even when `reviewPullRequest` had already returned successfully and a subsequent step (e.g. DB insert) threw. The same partial-capture pattern as `partialCostUsd` applies to all four: declare `partialTokensIn/Out/Grounding/FindingsCount` before the try and assign from `outcome` immediately after `reviewPullRequest` returns. `server/src/modules/reviews/run-executor.ts`
- **2026-06-27 [Mistake]** — `saveRunTrace` in the catch block was unconditional: if `saveRunTrace` in the success path threw (triggering the catch), the catch would call `saveRunTrace` again with a minimal buffer-only trace, overwriting the real detailed trace. Fix: move `saveRunTrace` inside the same `if (!runCompleted)` block as `completeAgentRun`. Both persistence calls in the catch path must be guarded together. `server/src/modules/reviews/run-executor.ts`
- **2026-06-27 [Mistake]** — Catch path used `costUsd: partialCostUsd ?? undefined` to distinguish "LLM never returned" (null → undefined → skip write) from "LLM returned with real cost". This is fragile: `null` and `undefined` carry different semantics but look identical at the call site. Fix: add explicit `let partialCostKnown = false`; set it `true` after `reviewPullRequest` returns; use `costUsd: partialCostKnown ? partialCostUsd : undefined`. Apply this pattern any time a nullable value must mean two different things (unknown vs known-null). `server/src/modules/reviews/run-executor.ts`
- **2026-06-27 [Mistake]** — `costRows` query in `GET /repos/:id/pulls` filtered by `prId IN (prIds)` and `status='done'` but omitted `workspaceId`. Safe in practice (prIds are already workspace-scoped), but diverges from the codebase pattern and becomes a real bug if prIds ever come from an unscoped source. Add `eq(t.agentRuns.workspaceId, workspaceId)` to the WHERE clause — every DB query in this module must scope by workspace. `server/src/modules/pulls/routes.ts`
- **2026-06-27 [Mistake]** — `costByPr.set(prId, null)` was called when Postgres `SUM()` returned NULL (all done runs have unknown cost). This conflates "key absent = no done runs" with "key present + null = runs exist but cost unknown", breaking any future consumer that uses `map.has(prId)` to distinguish the two cases. Fix: only call `costByPr.set()` when `parsed !== null && Number.isFinite(parsed)` — absent key and null-sum both correctly produce `null` downstream via `costByPr.get(prId) ?? null`. Map type narrows from `Map<string, number | null>` to `Map<string, number>`. `server/src/modules/pulls/routes.ts`
- **2026-06-27 [Quirk]** — The AI reviewer repeatedly flags `runBus.complete` placement, pricing.ts/SQL sync, and workspace-filter issues even after they are fixed, because it reads only the diff context and may not see the current file state. Always verify reviewer findings against the actual file before acting. Add explicit inline comments (like "Always release the SSE stream — even when runCompleted=true") to make intent unmistakable and reduce future re-flagging. `server/src/modules/reviews/run-executor.ts`
- **2026-06-27 [Mistake]** — `runCompleted = true` was set after `completeAgentRun` but BEFORE `saveRunTrace`. If `saveRunTrace` threw, the catch block's `if (!runCompleted)` guard prevented re-writing the error status, leaving the run permanently `status='done'` with no trace document. Fix: set `runCompleted = true` only after BOTH writes succeed. `server/src/modules/reviews/run-executor.ts`
- **2026-06-27 [Mistake]** — Fire-and-forget Fastify handlers that use `req.log` in background tasks log to a recycled object. Fastify recycles the request (and its logger) as soon as the response is sent. Fix: `const log = req.log.child({ route: '…', repoId: … })` before the async work begins, then pass `log` to all background calls. `server/src/modules/reviews/routes.ts`
- **2026-06-27 [Decision]** — `getBrief` now takes `workspaceId` and adds `INNER JOIN pull_requests ON pr_brief.pr_id = pull_requests.id WHERE pull_requests.workspace_id = $workspaceId`. The route handler already verified workspace ownership before calling it, but the query itself being unscoped violated the module convention (every DB query must filter by workspace). Defense-in-depth: the query is self-contained and safe regardless of calling code. `server/src/modules/reviews/repository/pull.repo.ts`
- **2026-06-27 [Decision]** — `POST /repos/:id/review-all` rate-limited to 2 requests/minute per workspace (`config: { rateLimit: { max: 2, timeWindow: '1 minute' } }`). Each call fans out to all open PRs, so a single call can already queue dozens of LLM runs; 2/min prevents repeated fan-outs without hampering normal use. `server/src/modules/reviews/routes.ts`
- **2026-06-30 [Mistake]** — When adding the `intent` spread to the `reviewPullRequest()` options object in `run-executor.ts`, the existing `skills` spread was deleted and replaced instead of being extended alongside it, silently dropping skills from all agent runs. The options object is additive — each slot spread is independent. When adding a new slot, extend: add the new spread without removing existing ones. `server/src/modules/reviews/run-executor.ts`
- **2026-06-30 [Mistake]** — `GET /agents` returns `Agent[]` with no `skill_count`; `AgentCard` accepts `skillCount?: number` and renders a Sparkles badge, but both callers (`AgentsListView.tsx:86`, `agents/[id]/page.tsx:84`) never pass it — the badge is silently hidden by `{skillCount != null && (...)}`. Fix: LEFT JOIN + COUNT from `agent_skills` in `AgentsRepository.list()`, extend shared `Agent` contract with `skill_count: number`, update `toAgentDto`, then pass `skillCount={a.skill_count}` in both call sites. `server/src/modules/agents/repository.ts:54`, `client/src/app/agents/_components/AgentsListView/AgentsListView.tsx:86`, `client/src/app/agents/[id]/page.tsx:84`
- **2026-07-02 [Mistake]** — `AgentStats` is ALREADY exported from `contracts/observability.ts` (a rich per-agent detail contract reserved for a future `GET /agents/:id/stats` — runs, findings_by_severity, trend, latency). Adding a second same-named export in `contracts/knowledge.ts` compiles the file fine but breaks the barrel with `TS2308: Module './contracts/knowledge.js' has already exported a member named 'AgentStats'` at `index.ts`. Before naming any new `@devdigest/shared` export, grep the WHOLE `vendor/shared` dir for the name. The Agents list-card footer rollup was named `AgentCardStats` to stay distinct. `server/src/vendor/shared/contracts/observability.ts:96`, `server/src/vendor/shared/contracts/knowledge.ts`
- **2026-07-11 [Mistake]** — Narrowing `reviewsForPull` (`GET /pulls/:id/reviews`) / `listRunsForPull` (`GET /pulls/:id/runs`) to exclude multi-agent fan-out runs (`agent_runs.multi_agent_run_id IS NULL`, commit `a529535`) silently regressed the Multi-Agent Review page. Its trace drawer AND TabsView source FULL finding detail from `usePrReviews(pr_id)` → this very endpoint (`MultiAgentResultsView` builds `reviewsByRunId` from it, then `RunTraceDrawer findings={reviewsByRunId.get(runId)?.findings ?? []}`) — NOT from the multi-agent query. Symptom: the trace modal's Findings section shows "No findings for this run" (badge 0) while the ColumnsView card and the modal's Stats→FINDINGS count still show the right number (those read `useMultiAgentRun`→`AgentColumn.findings` and persisted `trace.stats.findings` respectively, both untouched by the commit). Note `AgentColumn.findings` is only a SUBSET of `FindingRecord` (no `rationale`/`suggestion`/`end_line`), which is why the drawer deliberately used the richer `usePrReviews` source and can't just fall back to `col.findings` without losing detail. The commit message's "multi-agent results are unaffected" was wrong: only ColumnsView reads the dedicated multi-agent query. Before narrowing any PR-scoped reviews/runs query, grep for other consumers of the same client hook. Proper fix: expose `multi-run.repo findingsAndReviewsForRuns` as its own endpoint/hook and decouple the multi-agent page from `usePrReviews`. `server/src/modules/reviews/repository/review.repo.ts:70`, `client/src/app/multi-agent-review/[runId]/_components/MultiAgentResultsView/MultiAgentResultsView.tsx:184`

## Decisions
<!-- Architectural or design choices with the reasoning behind them. -->
- **2025-06-01 [Decision]** — `process.env.OPENAI_API_KEY` is `undefined` even when set in `.env`; secrets flow through `SecretsProvider`, which reads `~/.devdigest/secrets.json` first. In services, get secrets via `container.secrets.get('openai')` — never read `process.env` for API keys directly.
- **2025-06-01 [Decision]** — The entire run log (PromptAssembly + RunLogLine[] + stats + grounding summary) is stored as a single `run_traces.trace` JSONB column — there are no per-event rows. To inspect a run: `SELECT trace FROM run_traces WHERE run_id = '...'`. `server/src/db/schema.ts`
- **2025-06-01 [Decision]** — `runBus` is an in-memory event emitter; a client on one API instance won't receive events from a review running on another. Horizontal scaling would require replacing `runBus` with Redis Pub/Sub or an external bus — a known architectural limitation.
- **2026-06-27 [Decision]** — `server/tsconfig.json` sets `rootDir: ".."` (monorepo root) intentionally. The `@devdigest/reviewer-core` path alias resolves to `../reviewer-core/src/`, which TypeScript includes transitively for emission; `rootDir: "src"` errors with TS6059, and the suggested alternative `rootDir: "../reviewer-core"` is also invalid because `server/src/**` is not under `reviewer-core/`. The only alternatives are `rootDir: ".."` (current) or setting up TypeScript project references so reviewer-core emits `.d.ts` files separately. The output shift (`dist/server/src/server.js`) is intentional — `package.json` `start` script reflects this. `server/tsconfig.json:18`
- **2026-06-27 [Decision]** — Migration 0009 dropped `cost_usd` from `agent_runs`; migration 0010 re-added it (L01 cost badge feature). Runs written in that window have NULL `cost_usd` despite having valid `tokens_in`/`tokens_out`/`model`. Migration 0011 backfills those rows using the pricing table embedded in the SQL. When adding/removing a cost column, always check for this gap and include a corresponding backfill migration. `server/src/db/migrations/0011_backfill_cost_usd.sql`
- **2026-06-27 [Decision]** — The Stop hook for auto-triggering `/engineering-insights` uses a sentinel file at `/tmp/cc-ins-{session_id}` to prevent an infinite loop. Without it: hook fires → Claude runs insights → Claude stops → hook fires again → repeat forever. The sentinel is created on first fire; subsequent firings in the same session output nothing, so Claude truly stops. `.claude/hooks/stop-insights.sh`, `.claude/settings.json`
- **2026-06-30 [Decision]** — Added `server-testing` skill (`.claude/skills/server-testing/SKILL.md`) to provide Fastify/Drizzle/Vitest test patterns for `server/`. Previously no backend testing skill existed; `react-testing-library` only covers frontend. When writing tests for any file under `server/`, load `server-testing` — never `react-testing-library`. `.claude/skills/server-testing/SKILL.md`
- **2026-07-10 [Decision]** — `ci_runs.github_run_id`'s unique index (`schema/ci.ts`) is intentionally NOT scoped by `workspace_id` — only `ci_installations_workspace_repo_uq`/`ci_runs_workspace_id_idx` are workspace-aware — yet two different workspaces' `ci_installations` rows CAN legitimately point at the same external repo string (the Export-to-CI plan's own §1 design note). A literal `INSERT ... ON CONFLICT (github_run_id) DO UPDATE` (as the plan's Step 4 text shows) would let workspace B silently overwrite workspace A's row data on a real GitHub run-id collision. Fix: add `setWhere: eq(t.ciRuns.workspaceId, workspaceId)` to `onConflictDoUpdate` — Postgres then skips the update (no row returned) instead of touching a row the caller's workspace doesn't own, surfacing as a loud `ConfigError` rather than silent cross-tenant corruption. Any future direct write to `ci_runs` keyed only by `github_run_id` needs the same guard. `server/src/modules/ci/repository/runs.repo.ts`
- **2026-07-10 [Decision]** — `IngestService.resolveArtifact` (the `checkForNewResults` check loop, Step 8) wraps `listRunArtifacts` + `downloadArtifact` + `parseResultArtifact` in ONE try/catch degrading to `artifact = null` on ANY failure — not just the literal "no name match" case the plan's Step 8 text spells out. Rationale: `listWorkflowRuns` always returns the last 10 runs regardless of age, so an uncaught `downloadArtifact` throw (e.g. an expired artifact — GitHub's retention is commonly far shorter than "forever") would re-throw on EVERY future `checkForNewResults` call for the WHOLE workspace until that one run ages out of the 10-run window, aborting ingestion for every OTHER installation too (the loop is sequential; one throw exits the whole call). The plan's own "if no artifact matches by name, treat identically to a download failure" phrasing only makes sense if "a download failure → null" was already assumed as established behavior — this decision makes that assumption real, mirroring `parseResultArtifact`'s own documented never-throw contract one level up. If a future reviewer wants stricter fidelity to the literal spec text, this is the one place to look. `server/src/modules/ci/ingest-service.ts` (`resolveArtifact`)
- **2026-07-11 [Decision]** — `ExportService.bulkUpdate` ("Update CI config") now DOES open a pull request (find-or-open, mirroring single-export's AC-9/AC-10 path) — a DELIBERATE, user-approved deviation from AC-40's literal "SHALL NOT open a new pull request." Root cause of the "update config updates but nothing changes" bug: AC-40 assumed the original export PR stays open, but the real demo flow MERGES it (and GitHub may delete the `devdigest/ci` head branch on merge). After that, bulk-update's `commitFiles` alone stranded the regenerated config on a branch with no open PR — the running workflow kept the OLD config while the UI still reported "N updated". Fix commits to `CI_BRANCH` then `findOpenPr` → reuse if open (AC-10, so repeat clicks never stack duplicate PRs), else `openPullRequest` (AC-9); `pr_url` is now populated on each `CiBulkUpdateOutcome` and surfaced as a "View PR" link in `BulkUpdateResults.tsx` (new `ci.json` key `ciTab.viewPr`). NOTE the separate, unrelated failure mode that looks identical in the UI: "0 of N updated" with `HttpError: Resource not accessible by personal access token … git/trees#create-a-tree` is a TOKEN write-scope problem (needs a classic PAT with `repo`+`workflow` on those repos), NOT this code path — don't conflate the two. Tests: `ci-export-service.test.ts` flipped its old "never calls openPullRequest" assertion + added a reuse-existing-PR case; `CITab.test.tsx` asserts the View-PR link. `server/src/modules/ci/export-service.ts` (`bulkUpdate`), `client/src/app/agents/[id]/_components/AgentEditor/_components/CITab/_components/BulkUpdateResults.tsx`

## Quirks
<!-- Dependency gotchas, env constraints, non-obvious tool or library behavior. -->
- **2025-06-01 [Quirk]** — `*.it.test.ts` files pull a Postgres Docker image and create an ephemeral DB per suite; Docker must be running and the first run is slow due to image pull. Use `pnpm exec vitest run --exclude '**/*.it.test.ts'` for fast hermetic-only runs without Docker.
- **2025-06-01 [Quirk]** — All lesson tables (L02–L08) are created by the initial migrations and appear empty until the lesson's module is registered in `src/modules/index.ts`. Empty tables for unimplemented lessons are expected. `server/src/modules/index.ts`
- **2026-06-26 [Quirk]** — Old JSONB documents omit keys that didn't exist when written; `.nullable()` expects the key present with `null`, so parsing an old document that lacks the key fails. For any field added to a JSONB-deserialized schema, use `.nullish()` (= optional + nullable). `server/src/vendor/shared/contracts/trace.ts:68`
- **2026-06-26 [Quirk]** — Native platform packages installed inside WSL (Linux ABI) fail in a Windows Node.js process with `Cannot find module '@rollup/rollup-win32-x64-msvc'`. If `node_modules` were installed in WSL, run `pnpm install` from a Windows shell before running tests or migrations.
- **2026-06-27 [Quirk]** — `python3` in Git Bash on Windows resolves to the Microsoft Store stub and exits with code 49 instead of running Python. For inline JSON processing in `.claude/hooks/` scripts, use `node -e "..."` instead — `node` is reliably on PATH via the project Node install. `.claude/hooks/stop-insights.sh`
- **2026-06-27 [Quirk]** — Embedding a bash command that echoes JSON inside a settings.json `"command"` string fails the Claude Code validator ("Unterminated string") because `\"` escapes inside bash single-quoted strings conflict with JSON string parsing. Fix: extract logic to a separate `.claude/hooks/*.sh` file and set `"command": "bash .claude/hooks/script.sh"` — no escaping needed. `.claude/settings.json`
- ~~**2026-06-28 [Quirk]** — The `skills` prompt slot is already fully implemented end-to-end in reviewer-core: `ReviewInput.skills?: string[]` (`review/run.ts:56`), `assemblePrompt({ skills })` (`prompt.ts:88`), and `PromptAssembly.skills` in the trace schema all exist. The ONLY missing wire is in `run-executor.ts:runOneAgent()` — fetch `agentsRepo.linkedSkills(agent.id)`, filter to `skill.enabled === true`, map to `.body`, pass as `skills: string[]` to `reviewPullRequest()`. Do not touch reviewer-core; the change is ~5 lines in `server/src/modules/reviews/run-executor.ts`.~~ (superseded 2026-06-30 — skills wiring is now implemented; see Mistake entry 2026-06-30 for the regression pattern)
- **2026-06-30 [Mistake]** — Adding a required field to a Zod schema in `@devdigest/shared` (e.g. `findingsCount: z.number().int()` to `SmartDiffFile`) immediately breaks any existing test that parses a fixture without that field (`test/contracts.test.ts`). Always update the corresponding test fixture when adding required fields to shared contracts. `server/test/contracts.test.ts:108`, `server/src/vendor/shared/contracts/brief.ts`
- **2026-06-30 [Pattern]** — Sub-plugins for large route files: when a `routes.ts` exceeds ~300 lines, create a new peer plugin file (e.g. `smart-diff.routes.ts`) and register it as a top-level module in `modules/index.ts`. The new plugin receives the same `app.container` and `getContext` call, and re-instantiates `ReviewService` identically. Avoid adding more routes to an already-large plugin. `server/src/modules/reviews/smart-diff.routes.ts`, `server/src/modules/index.ts`
- **2026-06-30 [Pattern]** — `ReviewService` constructor now accepts an optional `logger?: Logger` (defaulting to `console`). Use `new ReviewService(container, req.log)` in route handlers that need structured logging from the service layer. The fallback to `console` means callers without a Fastify request logger (e.g. tests) work unchanged. `server/src/modules/reviews/service.ts:40`
- **2026-07-02 [Pattern]** — Hermetic route tests with mock Drizzle db: provide `overrides: { auth: MockAuthProvider }` to bypass the seed requirement for workspace/user, then swap `(app.container as any).db` with a call-counted fake that returns fixture rows in order (select call 0=PR, 1=files, 2=prior PRs, 3=repo). The key is that the service's BlastRepository is lazily created (`_repo` field, null until first request), so patching the container's db before the first inject call works correctly. `server/test/blast-routes.test.ts`
- **2026-07-02 [Pattern]** — Cross-module type isolation for facade return types: instead of `import type { BlastResult } from '../repo-intel/types.js'` (forbidden cross-module import), derive the type from the container: `type BlastResult = Awaited<ReturnType<Container['repoIntel']['getBlastRadius']>>`. This satisfies the module isolation rule while remaining fully type-safe. `server/src/modules/blast/service.ts`
- ~~**2026-07-02 [Quirk]** — `indexer-pipeline.test.ts` and `conventions-extractor.test.ts` fail on Windows with `ENOENT` when creating files in `C:\Users\...\AppData\Local\Temp\` subdirectories. This is a pre-existing Windows-specific issue (likely a path-separator problem in `mkdirSync`). These 8 tests are excluded from the "all hermetic tests pass" invariant on this machine; CI runs on Linux where they pass. `server/test/indexer-pipeline.test.ts`, `server/test/conventions-extractor.test.ts`~~ (superseded 2026-07-11 — root-caused + fixed for indexer-pipeline; the conventions-extractor claim was inaccurate. See entry below.)
- **2026-07-11 [Mistake]** — The `indexer-pipeline.test.ts` Windows `ENOENT` (entry above) was a real bug in the test's own `writeFileAt` helper, not an environment quirk: it derived the parent dir with `const slash = full.lastIndexOf('/'); if (slash > 0) await mkdir(full.slice(0, slash), …)`. `path.join` emits `\` separators on Windows, so `lastIndexOf('/')` returns `-1`, the `slash > 0` guard is false, `mkdir` is skipped, and `writeFile` then throws `ENOENT` because the parent (e.g. `src`) was never created — 6–8 tests fail deterministically (not a flake). Fix: `await mkdir(dirname(full), { recursive: true })` (`path.dirname` is separator-agnostic); all 11 tests now pass on Windows. Any test helper that splits a `path.join` result on a literal `'/'` carries the same latent bug — grep for `lastIndexOf('/')` / `split('/')` over path-string code. The old entry's `conventions-extractor.test.ts` half is inaccurate: that file imports only pure functions (`buildDedupKey`, `isSafeEvidencePath`) and never touches the filesystem, so it cannot fail this way. `server/test/indexer-pipeline.test.ts:140`
- **2026-07-02 [Quirk]** — `repos.clone_path` stored as a WSL path (`/mnt/f/...`) is unreachable when the API later runs as a Windows process: repo-intel silently degrades to `no_data` (0 files indexed, blast empty) while reviews still work (diff comes via the GitHub API, not the clone). Fix: re-clone into `server/clones/<owner>/<name>` on the Windows path, `UPDATE repos SET clone_path=...`, then `POST /repos/:id/resync`. Check `clone_path` first whenever index-state shows `no_data` for a repo that "should" be indexed. `server/src/modules/repo-intel/service.ts`
- ~~**2026-07-02 [Quirk]** — All `*.it.test.ts` currently fail in `beforeAll` at `seed(db)` with `PostgresError: column "pseudocode_summary" of relation "pr_files" does not exist`. The schema declares `prFiles.pseudocodeSummary` (`schema/pulls.ts:45`) but no migration adds the column, so a freshly-migrated testcontainer lacks it and the seed's INSERT explodes. Pre-existing drift affecting every integration suite (verified on `agents-versions.it.test.ts`), not any one feature. Fix path: `pnpm db:generate` to emit the missing column migration. Until then, integration tests can't run locally. `server/src/db/schema/pulls.ts:45`, `server/src/db/seed.ts`~~ (superseded 2026-07-03 — migration `0013_context_documents.sql` now adds `pr_files.pseudocode_summary` via `ALTER TABLE ... ADD COLUMN IF NOT EXISTS`; the `IF NOT EXISTS` was required because this specific long-running local dev DB already had the column present out-of-band, outside `drizzle.__drizzle_migrations` tracking — see the Mistake entry below on the same migration)
- **2026-07-03 [Mistake]** — `server/src/db/migrations/meta/{0011,0012}_snapshot.json` were corrupt: `0011_snapshot.json` was a byte-identical clone of `0010_snapshot.json` including its `id` (both `a1b2c3d4-e5f6-7890-abcd-ef1234567890`, an obviously hand-crafted placeholder, not a drizzle-kit-generated UUID), and `0012_snapshot.json` was missing entirely even though `0012_conventions_extractor.sql` was applied and tracked in `_journal.json`. Root cause: migrations 0010–0012 were hand-authored (SQL written directly, `pnpm db:generate` never re-run to refresh the snapshot chain afterward). Symptom: `pnpm db:generate` fails immediately with `"are pointing to a parent snapshot... which is a collision"` before it ever reaches your actual schema diff. Fix: give the colliding snapshot a fresh `crypto.randomUUID()` id, hand-build the missing snapshot(s) from the actually-applied SQL + `information_schema` ground truth (not from the TS schema source alone — e.g. `conventions_repo_dedup_uq` in `knowledge.ts:93` has NO `.where()` clause even though the live DB's index IS partial, a second independent drift), then re-run `db:generate` and inspect the emitted diff is empty/expected before trusting the chain. Any time hand-authored SQL migrations exist (`0010_add_agent_run_cost_usd.sql`, `0011_backfill_cost_usd.sql`), assume the snapshot chain past that point is unreliable until proven otherwise with a dry-run `db:generate`. `server/src/db/migrations/meta/0011_snapshot.json`, `server/src/db/migrations/meta/0012_snapshot.json`
- **2026-07-03 [Mistake]** — After a `db:generate` run whose output you don't like (e.g. contains unwanted statements), deleting the generated `.sql` file alone is NOT enough to "undo" it — `drizzle-kit` also wrote a matching `_journal.json` entry and a `NNNN_snapshot.json` for that migration index. A second `db:generate` run then diffs against that orphaned snapshot as its new baseline and silently reports `"No schema changes, nothing to migrate"` even though real schema changes exist. Always delete all three artifacts together (the `.sql`, the `meta/NNNN_snapshot.json`, and the matching `_journal.json` entry) before regenerating. `server/src/db/migrations/meta/_journal.json`
- **2026-07-03 [Quirk]** — `GitClient.clonePathFor(repo)` (`SimpleGitClient`) ALWAYS computes `<cloneDir>/<owner>/<name>` from `RepoRef` — it never reads the stored `repos.clone_path` DB column. A repo row whose `clone_path` is set but points somewhere other than that convention path (e.g. the seeded `felkost/dev-digest` row, whose `clone_path` is the live monorepo root, not `server/clones/felkost/dev-digest`) silently produces empty results from any feature that calls `clonePathFor`/`currentHead`/`readFile` — no error, just `[]`, because those calls target a directory that doesn't exist under `cloneDir`. This is distinct from the WSL-path quirk above (that one has a real but *unreachable* path at the conventional location; this one has clone_path pointing somewhere the convention never looks at all). When smoke-testing anything that reads from a repo's clone, pick a seeded repo whose clone actually lives under `server/clones/<owner>/<name>` (verify with `docker exec devdigest-postgres psql -U devdigest -c "select owner,name,clone_path from repos"` cross-referenced against `ls server/clones/<owner>/`), not just any repo with a non-null `clone_path`. `server/src/adapters/git/simple-git.ts:37`, `server/src/modules/context-docs/service.ts`
- **2026-07-03 [Decision]** — `Db` here is `PostgresJsDatabase` (postgres-js driver), NOT `node-postgres` — `db.execute(sql...)` return shape differs between the two drivers (postgres-js returns the row array directly; node-postgres wraps in `{rows}`), so raw multi-table UNION queries via `db.execute()` are a type-shape trap. Prefer Drizzle's query-builder `union()`/`unionAll()` from `drizzle-orm/pg-core` over hand-written `UNION` SQL — it stays fully typed end-to-end and sidesteps the driver-specific `execute()` return shape entirely. `server/src/modules/context-docs/repository.ts`
- **2026-07-03 [Quirk]** — `GitClient.readFile(repo, path)` takes a REPO-RELATIVE path, not the absolute path `resolveConfinedPath()` returns — the two functions' path shapes don't compose directly. `context-docs/service.ts` already does this correctly via `path.relative(cloneRoot, confined)` before calling `readFile`; any new caller of `resolveConfinedPath` (e.g. `run-executor.ts`'s run-time injection) must do the same conversion (or, since the confined check only validates — never rewrites — a POSIX-style stored path, it's equivalent to just re-using the already-relative `storedPath` after confinement succeeds). Passing the absolute confined path straight to `readFile` silently fails to find the file (adapter treats it as relative to `cloneRoot` and double-prefixes). `server/src/adapters/git/simple-git.ts`, `server/src/modules/context-docs/helpers.ts`, `server/src/modules/reviews/run-executor.ts`
- **2026-07-03 [Mistake]** — `ContextDocsRepository.getContextFolders` returns `string[] | undefined` for a missing/cross-workspace repo, but the service-layer wrapper `ContextDocsService.getContextFolders` THROWS `NotFoundError` in that same case instead of returning `undefined` — the two layers have different contracts for the identical scenario. `run-executor.ts`'s `buildContextDocs` used to call the repository directly and treat `undefined` as `folders ?? []`; switching the call to `container.contextDocs.getContextFolders` (the DI-normalization fix for AP-4) would let that `NotFoundError` propagate into `buildContextDocs`'s outer catch, silently collapsing the whole method to `{specs:[], trace:[]}` and erasing the per-document `skipped` trace entries the tests assert on — a real behavior change even though the repo row is always pre-validated by the caller in practice. Fix: wrap the service call in a local `try/catch` that degrades to `folders = undefined` on any throw, preserving the exact prior semantics. Any time a repository method returning `T | undefined` is swapped for a service-layer method with the same name, diff their error contracts before assuming they're interchangeable. `server/src/modules/reviews/run-executor.ts`, `server/src/modules/context-docs/service.ts:191`, `server/src/modules/context-docs/repository.ts:70`
- **2026-07-03 [Pattern]** — When a `Container` facade getter (e.g. `container.contextDocs`) is added and an existing hermetic test builds its mock `Container` as a plain object literal (`{ db, git, ... } as unknown as Container`) rather than a real `Container` instance, the getter simply doesn't exist on that fake — accessing it silently returns `undefined`, and any code that calls a method on it throws `TypeError`, which upstream best-effort try/catch blocks (like `run-executor.ts`'s `buildContextDocs`) swallow into an empty result with NO assertion failure pointing at the real cause (symptom looked like "readFile never called" with no stack trace). Fix: in the test's container-builder helper, manually attach a real instance of the new facade service backed by the same fake `db`/`git` (mirrors how `blast` is already stubbed there) — `(container as unknown as { contextDocs: X }).contextDocs = new ContextDocsService(container)`. Any new `Container` getter requires auditing hermetic tests that fake the whole container as a plain object, not just ones that construct a real `Container`. `server/test/run-executor-context-docs.test.ts`
- **2026-07-03 [Quirk]** — `server/src/modules/context-docs/service.ts` (uncommitted, pre-existing) contained a literal NUL byte (`\x00`) in place of the space character inside `.join(' ')` (byte offset 883). `Read`/`grep`/`file` all still "worked" superficially — `Read` silently normalizes/hides it in its cat-n output, and `tsc`/Node parse it fine as a valid (if odd) string char — but the Edit tool's exact `old_string` match fails with no diagnostic beyond "String to replace not found" even when the visible text is byte-for-byte identical to the Read output. Diagnose with `node -e "const d=fs.readFileSync(p); console.log(d.indexOf(0))"` (not `python3` — resolves to the Store stub per the existing Quirk entry) whenever Edit inexplicably fails to match text that Read just displayed as identical; fix by splicing a real space `Buffer` in place of the NUL byte via `fs.writeFileSync`, then retry Edit normally. `server/src/modules/context-docs/service.ts:24`
- **2026-07-03 [Quirk]** — The hermetic `context-docs-service.test.ts` mock db is a plain promise-chain fake whose generic `select()` cannot see which table it targets, so it routes by inspecting the requested COLUMN KEYS (`select({count})` → agent count; `select({body,version})` → getOverlay; `select({path,body})` → listOverlays; else → the repo row). Adding a NEW `ContextDocsRepository` query whose column set collides with an existing branch — or isn't handled at all — silently falls through to the default "return the repo row" branch, so the new query receives a `{clonePath,owner,name,contextFolders}` row with no `path`, and the failure surfaces far away as `TypeError: Cannot read properties of undefined (reading 'replace')` inside `categoryFor` (the undefined path), NOT at the query site. When you add a repository method backed by a distinct column selection, add a matching `resolveFor()` branch keyed on its columns before extending the service tests. `server/test/context-docs-service.test.ts`
- **2026-07-03 [Quirk]** — `noUncheckedIndexedAccess` (server `tsconfig.json`) makes any `Record<string, T>` index access return `T | undefined`, not `T` — even inside a ternary like `path in fixtures ? fixtures[path] : null` (the `in` check does NOT narrow the indexed-access type). `tsc` fails with "Type 'string | undefined' is not assignable to type 'string | null'" only at the RETURN site, not at the index expression itself, which can be confusing. Fix: skip the `in` check and use `fixtures[path] ?? null` directly — cleaner and correctly typed. Applies to any new mock/fixture lookup by dynamic key against a typed `Record`. `server/src/adapters/mocks.ts`
- **2026-07-03 [Quirk]** — Calling `resolveFeatureModel(container, workspaceId, id)` from a NEW module's service (e.g. onboarding) makes an extra `db.select({key, value}).from(t.settings).where(...)` call under the hood (`getFeatureModelOverride`) that a hand-rolled fake `db` built only for that module's own repository queries won't anticipate. The fake's generic `select()` silently routes the settings query into the wrong branch (whatever the fallback resolves to), and the failure surfaces deep inside `rowsToSettings` as `TypeError: rows is not iterable` — not at the call site. Fix: extend the column-key-sniffing convention (`server/insights.md` 2026-07-03 `context-docs-service.test.ts` entry) with a `{key, value}` → `[]` branch (no workspace override configured) BEFORE adding any other branch, and make the chain itself `then`/`catch`-thenable — `getFeatureModelOverride` awaits the chain directly with no `.limit()`. Any new module that calls `resolveFeatureModel` in a hermetic test needs this extra branch. `server/src/modules/settings/feature-models.ts`, `server/test/onboarding-service.test.ts`
- **2026-07-03 [Quirk]** — `server/src/prompts/*.md` files are saved with CRLF (`\r\n`) line endings on this Windows checkout. A test asserting `prompt.toContain('some phrase that wraps across a source line')` can fail even when the phrase is visually one sentence in the file, because the wrap point is a literal `\r\n` inside the string being matched against a space-joined expected substring. Fix: normalize with `.replace(/\r\n/g, '\n')` (or `.replace(/\s+/g, ' ')` for a wrap-agnostic match) before asserting on rendered prompt content pulled from a `.md` template. `server/src/prompts/onboarding.system.md`, `server/test/onboarding-service.test.ts`
- **2026-07-03 [Quirk]** — `client/src/vendor/shared/` is a SEPARATE physical copy of `server/src/vendor/shared/` — not a symlink, not a build-time alias resolution. `client/tsconfig.json` maps `@devdigest/shared` to `./src/vendor/shared/index.ts` (its own local file tree), while `server/tsconfig.json` maps the same package name to `server/src/vendor/shared/index.ts`. No sync script exists in either `package.json`. Editing `server/src/vendor/shared/contracts/*.ts` alone does NOT make new exports available to client code — the same edit must be manually mirrored into `client/src/vendor/shared/contracts/*.ts` before any client file can `import { X } from "@devdigest/shared"`. When a plan step is scoped to only the server-side contracts file (e.g. "Step 1: shared contract additions"), later client-side steps that import the new types will fail client typecheck until someone copies the change across — check for this gap explicitly rather than assuming one edit covers both packages. `client/src/vendor/shared/contracts/brief.ts`, `server/src/vendor/shared/contracts/brief.ts`
- **2026-07-05 [Quirk]** — Changing the embedding model (e.g. `text-embedding-3-small` → `text-embedding-3-large`) silently breaks all pgvector similarity queries: `SELECT ... WHERE embedding <-> query_vec ORDER BY ...` returns **0 rows** even when matches should exist, because the stored vectors have a **different dimensionality** than the query vector (1536 vs 3072 dims). No schema error on INSERT (pgvector allows mismatched dimensions per row), no error on query (Postgres just compares vectors of different sizes and finds none in range). When migrating between embedding models of different output dimensions, EITHER (1) backfill all stored vectors with new embeddings from the new model, OR (2) add a new column and mark the old one deprecated. Tests against old fixture data with pre-computed vectors also fail the same way silently — ensure test vectors match the active embedding model's dimensionality. Symptom: "search results disappeared after an LLM provider upgrade" usually means embedding dimensionality shift. `server/src/modules/context-docs/repository.ts` **Re-diagnosed 2026-07-05** — this pattern loops repeatedly; root-cause check FIRST when pgvector searches suddenly go silent.
- **2026-07-03 [Decision]** — Confirmed `.for('update')` (row lock) works exactly as written against the installed `drizzle-orm@0.38.4`: `PgSelectBase.for(strength: LockStrength, config?: LockConfig)` where `LockStrength = 'update' | 'no key update' | 'share' | 'key share'` (`node_modules/drizzle-orm/pg-core/query-builders/select.types.d.ts:58`). No adaptation was needed for `pull.repo.ts`'s `upsertBrief`/`upsertLlmBrief` transactional rewrite (PR Why + Risk Brief plan, Step 2) — the plan's snippet was used verbatim. This was also the first use of `db.transaction()` anywhere in this module (confirmed via grep — no prior `db.transaction(` call existed in `src/`). `server/src/modules/reviews/repository/pull.repo.ts`
- **2026-07-03 [Mistake]** — When a budget-truncation function must report which sections were dropped "in priority order" (e.g. drop 7 → drop 6 → halve 3), pushing to the dropped-list INSIDE the section-builder function (in document-position order: history built before context_excerpts) silently produces `['history', 'context_excerpts']` instead of the required `['context_excerpts', 'history']` — the bug only surfaces when both are dropped in the same call, since single-drop cases look correct either way. Fix: maintain the dropped-order list OUTSIDE the builder, appended exactly at each drop DECISION site (the `if (over budget && includeX) { includeX = false; sectionsDropped.push('x'); rebuild(); }` block), never inside the section-assembly function itself. Any "assemble with priority-ordered fallback/drop reporting" function should keep the two concerns (what's in the document vs. the order decisions were made) in physically separate code paths. `server/src/modules/reviews/brief-generator-helpers.ts`
- **2026-07-03 [Quirk]** — `pull.repo.ts`'s `getPull`, `getRepo`, `getIntent`, and `getPrFiles` ALL call `db.select()` with NO column map (plain `select()`, not `select({...})`) — the established "sniff by requested column keys" hermetic-fake-db convention (`server/insights.md`'s `context-docs-service.test.ts`/`onboarding-service.test.ts` entries) cannot distinguish between them at all. A fake db built for `BriefGeneratorService` needs a SEPARATE call-order counter for these four no-column-map calls specifically, in the FIXED order `generate()` issues them (getPull → getRepo → getIntent → getPrFiles) — reusing that same counter/db instance across two sequential `generate()` calls in one test (e.g. asserting an error twice) desyncs the index on the second call and produces a confusing downstream crash (`Cannot read properties of undefined (reading 'join')` inside `buildIntentSection`) far from the real cause. Fix: build a FRESH fake db per `generate()` invocation in any test that calls it more than once, or wrap the single call in `try/catch` instead of calling `generate()` twice. `server/test/brief-generator-service.test.ts`
- **2026-07-03 [Quirk]** — Patching `blast`/`contextDocs` on a REAL `Container` instance (e.g. one returned by `buildApp()` in a route-level hermetic test, as opposed to a hand-built plain-object fake container) with a plain assignment (`(app.container as any).blast = {...}`) throws `TypeError: Cannot set property blast of #<Container> which has only a getter` — `Container`'s `blast`/`contextDocs`/`repoIntel` are lazily-memoized getter-only accessors (`private _blast?: BlastService; get blast() {...}`), not plain writable properties. Fix: `Object.defineProperty(app.container, 'blast', { value: {...}, configurable: true })` instead of assignment. This is distinct from the existing entry below (that one covers a plain-object-literal FAKE container built by hand, where assignment works fine because there's no getter in the way) — the fix differs depending on whether the container under test is a real `Container` instance or a hand-built object literal. `server/test/brief-routes.test.ts`, `server/src/platform/container.ts:139`
- **2026-07-03 [Mistake]** — In a route-level hermetic test built via `buildApp({ overrides: { llm: { openrouter: mockLlm } } })`, registering the mock under the wrong provider key silently lets a real (non-mocked, network-calling) LLM adapter answer instead — no error, no crash, just an unexpectedly "real-sounding" response that fails a fixture-equality assertion far from the root cause. `resolveFeatureModel(container, workspaceId, 'risk_brief')` resolves to the **`openai`** provider by default (`contracts/platform.ts`'s `FEATURE_MODELS` entry for `risk_brief`), not `openrouter` (which is `onboarding`'s default). Before registering a `MockLLMProvider` in any route/service test, check which provider the specific `FeatureModelId` actually defaults to in `FEATURE_MODELS` — do not assume `openrouter` project-wide. `server/src/vendor/shared/contracts/platform.ts:59`, `server/test/brief-routes.test.ts`
- **2026-07-03 [Mistake]** — `upsertLlmBrief`'s "no existing `pr_brief` row" branch (`pull.repo.ts`) persists a deliberately-EMPTY blast shell (`{ changed_symbols: [], downstream: [], summary: '' }`) — it does NOT pull in whatever `blastResponse` the calling `BriefGeneratorService.generate()` gathered in the same request. An integration test that calls `POST /pulls/:id/brief` on a brand-new PR with no prior `upsertBrief` call will get a 200 with a populated `llm` field but an EMPTY `blast.changed_symbols`, even though `container.blast.getBlast` was mocked to return real symbols — because the persisted brief came from the empty-shell branch, not from the request's gathered facts. This is intentional (the shell is a "don't lose the LLM part" placeholder for the edge case "PR never reviewed"), not a bug, but it means any happy-path integration test asserting on `blast.*` fields in the POST response MUST first call `upsertBrief(db, prId, ...)` (simulating a real prior review's auto-composition) before calling `POST /pulls/:id/brief` — otherwise the assertion fails with an empty array and the failure looks like a blast-mocking wiring problem, not a test-setup ordering problem. `server/src/modules/reviews/repository/pull.repo.ts:147-159`, `server/test/brief-generator.it.test.ts`
- **2026-07-03 [Decision]** — Confirmed real Testcontainers Postgres row-lock concurrency test for `upsertBrief`/`upsertLlmBrief` (PR Why + Risk Brief plan, Step 10): firing both via `Promise.all` on the same `prId` with `.for('update')` in each transaction reliably serializes so BOTH the new deterministic fields (`blast.summary`) AND the new `llm` field survive in the final row — no lost update, confirming the Step 2 row-lock implementation is correct under real overlapping transactions, not just hermetically plausible. `server/test/brief-generator.it.test.ts`
- **2026-07-05 [Quirk]** — `pnpm db:generate` always names the emitted `.sql` file with drizzle-kit's own random adjective-noun pair (e.g. `0016_typical_angel.sql`), never the descriptive name a plan or convention expects. To rename to a descriptive name (e.g. `0016_eval_batches.sql`), you must ALSO edit `meta/_journal.json`'s matching entry's `"tag"` field to the new filename stem — drizzle-kit resolves journal→SQL by exact tag-to-filename match at migrate time, so a rename without the journal edit orphans the SQL file silently (migrate would report "no migrations to apply" or skip it). The `meta/NNNN_snapshot.json` file itself is NEVER renamed to the descriptive form — it always stays plain `NNNN_snapshot.json` regardless of the SQL/tag name, unlike the `.sql`/journal-tag pair. `server/src/db/migrations/meta/_journal.json`, `server/src/db/migrations/0016_eval_batches.sql`
- **2026-07-03 [Decision]** — `validateReferences`'s internal `line: entry.line ?? 1` defaulting was REMOVED (now passes `entry.line` through as-is, possibly `undefined`) because `1` is almost never a real changed line and was silently poisoning both the stored `line` field AND the `github_link` `#L{line}` for every risk/review_focus reference where the LLM omitted a line number. The 1-default now lives ONE layer up, in `BriefGeneratorService.generate()`'s `resolveEnrichment()`: trust a real LLM line (`> 1`) as-is, else look up `firstChangedLineFromPatch(prFilesRow.patch)` per resolved file (computed once into a `Map<path, number>` before the enrichment loop), else fall back to whatever existed. Any function that "resolves" a value AND supplies a hardcoded numeric/sentinel default in the same step should be reconsidered — split resolution (can this reference be matched at all) from defaulting (what's a MEANINGFUL fallback value), since the caller usually has more context (here: per-file diff patches) to supply a better default than the pure helper does. This changed the tested contract of 12 existing `validateReferences` assertions (`line: 1` → `line: undefined`) — a warning sign that a "just remove `?? 1`" change looks trivial but is actually a public-contract change requiring a full test-suite re-check. `server/src/modules/reviews/brief-generator-helpers.ts`, `server/src/modules/reviews/brief-generator.ts`
- **2026-07-05 [Decision]** — For L06 eval `service.ts`/`run-orchestrator.ts`'s cross-module reads (finding→PR→diff lookup for AC-5, agent config+skills lookup for the batch runner), NO new `Container` facade was needed despite the plan anticipating one. `container.reviewRepo` (full `ReviewRepository`, exposing `findingContext(findingId)` → `{finding, review, pull}` and `getPrFiles(prId)`) and `container.agentsRepo` (exposing `getById`/`linkedSkills`) are ALREADY general-purpose cross-cutting facades — `container.ts`'s own comment says so ("Shared repositories for cross-cutting entities... so consuming modules use `container.agentsRepo` instead of reaching into another module's folder"), and `reviews/routes.ts` itself already calls `container.reviewRepo` externally. Before adding a NEW lazy-getter facade (the `blast`/`contextDocs` precedent), check whether `container.reviewRepo`/`container.agentsRepo` already expose the exact minimal read needed — they cover most "read another module's row" cross-cutting needs already. `server/src/platform/container.ts:105-111`, `server/src/modules/eval/service.ts`, `server/src/modules/eval/run-orchestrator.ts`
- **2026-07-05 [Quirk]** — `@devdigest/shared`'s `Finding` schema uses `start_line`/`end_line` (snake_case, LLM-facing wire format), but the DB row type `FindingRow` (`db/rows.ts`, from `findings` Drizzle table) uses `startLine`/`endLine` (camelCase), and `eval/scoring.ts`'s pure `FindingLike` interface also uses `startLine`/`endLine`. Any code bridging a `ReviewOutcome.review.findings` (LLM `Finding[]`, snake_case) into `scoreCase()`/`matchesExpectation()` (camelCase `FindingLike`) needs an explicit per-field remap (`{file: f.file, startLine: f.start_line, endLine: f.end_line}`) — there is no shared type alias covering both naming conventions. `server/src/vendor/shared/contracts/findings.ts:47-63`, `server/src/db/rows.ts:14`, `server/src/modules/eval/scoring.ts:15-19`
- **2026-07-06 [Mistake]** — L06 eval-case seed `expected_output` `line_start`/`line_end` MUST use NEW-side diff line numbering, and `parseUnifiedDiff` (reviewer-core diff-parser) does NOT advance the new-side counter for deleted (`-`) lines — only context and added (`+`) lines advance it. Authoring expectations by counting deleted lines as consuming new-side numbers pushes every pinpoint expectation past its target: `must_find` never matches (recall silently reads 0), `must_not_flag` zones drift off the guarded code (the precision guard becomes vacuous). Nothing throws — the demo just shows broken recall, and a seed comment claiming "parser-verified numbering" was actually false. When authoring or reviewing eval fixtures, trace each expectation's range through the parser's new-side rule against that case's own `input_diff`; a cheap seed-time assertion that each range falls within the diff's new-side line count catches regressions. `server/src/db/seed.ts:795-916`, `reviewer-core` diff-parser `parseUnifiedDiff:63-74`
- **2026-07-06 [Mistake]** — A `pnpm db:generate` run that fails PARTWAY THROUGH (here: `EPERM`/file-lock on `meta/_journal.json` on a Windows checkout, likely OneDrive/AV holding a handle) can still have written the NEW `meta/NNNN_snapshot.json` before the failure, while the journal entry and `.sql` file are never created — the inverse of the already-documented "delete artifacts together" quirk (that one covers a deliberately-unwanted generate; this one covers a genuinely-crashed one). Symptom: the immediate retry reports "No schema changes, nothing to migrate" even though the schema edit is real and unapplied, because the orphaned snapshot is silently adopted as the new baseline. Fix: before retrying, check for an `meta/NNNN_snapshot.json` one index ahead of the last committed journal entry with NO corresponding journal entry — delete it, then retry `db:generate` cleanly. `server/src/db/migrations/meta/_journal.json`
- **2026-07-06 [Decision]** — When a batch-run API endpoint moves from synchronous-response to 202-Accepted-plus-detached-execution (fire-and-forget, mirroring `review-all`'s precedent), split the orchestrator method in two: a fast, still-`await`-able `startBatch()` (validates target, inserts the placeholder row, returns fast) and a separate `executeBatch()` that does the actual fan-out + seals the aggregate — callers that want the OLD synchronous all-in-one behavior (tests, non-route callers) get a thin `runBatch()` wrapper composing both. This keeps 100% of the existing scoring/aggregation logic byte-for-byte reusable and lets the route handler `void executeBatch(...).catch(...)` without needing a parallel code path. `server/src/modules/eval/run-orchestrator.ts`
- **2026-07-06 [Quirk]** — A finalized/frozen `@devdigest/shared` contract (e.g. `EvalBatchCompareResult.deltas.recall: z.number()`, deliberately non-nullable) can conflict with a "make X null-aware, don't fabricate deltas vs null" bugfix request — you cannot literally return `null` for a field the Zod response schema declares non-nullable; `fastify-type-provider-zod` will throw at serialize time in production, not just fail a test. Resolution ladder when this happens without contract-edit permission: (1) push the exclusion further upstream (e.g. filter out all-null-metric batches from `getTrend`'s array entirely, so the never-null contract is never asked to represent a null case) or (2) throw a `ValidationError` explaining why the two things can't be compared, rather than silently coercing null→0 (the original bug). Never silently violate the frozen contract's nullability by returning null anyway — the type checker won't catch this since the return type is inferred, not the Zod schema. `server/src/modules/eval/service.ts` (`getTrend`, `compareBatches`)
- **2026-07-06 [Quirk]** — Two modules can independently define an *identically-shaped* pino-compatible `Logger` type (`{info,warn,error,debug}`) — this is NOT a violation worth deduplicating via cross-module import. Module import isolation (R6 in `backend-onion-architecture`) forbids importing even a `type`-only symbol from another module's file (e.g. `import type { Logger } from '../reviews/run-executor.js'` inside `eval/run-orchestrator.ts`), because TypeScript type-only imports still couple the two modules' file graphs and violate the "a module imports only from its own files / shared / container / db / _shared" rule literally. Redeclare the same minimal structural type locally instead — cheap, and keeps modules truly independent. `server/src/modules/eval/run-orchestrator.ts`, `server/src/modules/reviews/run-executor.ts`
- **2026-07-06 [Pattern]** — A hermetic test for a repository method whose correctness hinges on genuine row filtering (e.g. `clearHistory`'s "delete THIS agent's rows, not a different agent's/workspace's") cannot be verified with the codebase's usual call-recording spy chain (`selectChain`/`makeSelectOnlyDb`) — that convention only proves a `where()` was called, not that it filters correctly. Instead, build a small in-memory fake `db` with real row arrays, and extract the actual `{column: value}` pairs out of the REAL `eq()`/`and()` drizzle-orm `SQL` objects passed to `where()`: each `eq()` result's `queryChunks` is `[StringChunk, Column, StringChunk(' = '), Param{value}, StringChunk]` (Column at index N → bound value at index N+2, unwrap `.value` if the following chunk is a `Param`-like wrapper), and `and()` nests each `eq()` SQL as its own `queryChunks` entry joined by `StringChunk(' and ')` — recurse into any `queryChunks`-bearing object. `inArray(column, subqueryPromise)` wraps the subquery similarly inside a `Param.value` (not directly thenable at the chunk), so unwrap `.value` before checking `typeof x.then === 'function'`. Real column names surface as snake_case (`workspace_id`) even though the fixture rows use the Drizzle JS field names (camelCase, `workspaceId`) — convert before comparing. This makes the fake db a genuine (if minimal) predicate evaluator instead of a scripted stub, catching real filtering bugs a spy-chain would miss. `server/test/eval-repository.test.ts` (`makeStatefulEvalDb`, `extractEqPairs`)
- **2026-07-06 [Pattern]** — Adding a new nullable column to a Drizzle table (e.g. `eval_runs.error_message`) does NOT require touching the repository method's signature when that method already takes a generic `typeof t.tableName.$inferInsert` param (e.g. `EvalRepository.insertRun(data: EvalRunInsert)`) — the inferred insert type absorbs the new optional field automatically, and any caller can just add the key to the object literal it already builds. No repository/service plumbing needed; only the schema, the call sites building the insert object, and the DTO mapper that reads the row back (here `helpers.ts`'s `batchCaseOutcome`) need edits. Before assuming a "plumb a new field through" task requires a signature change, check whether the touched method already accepts the row's full inferred insert/select type. `server/src/modules/eval/repository.ts` (`insertRun`), `server/src/modules/eval/run-orchestrator.ts`
- **2026-07-07 [Quirk]** — Every pre-existing route in `eval/routes.ts` omits a `response` schema entirely (bare `{ schema: { params: ... } }`), so Fastify never validates/serializes the return value against a Zod shape — it just JSON.stringifies whatever the service returns. Adding the FIRST route with `response: { 200: z.array(EvalBatch-derived-contract) }` (e.g. `GET /evals/overview`/`GET /evals/recent`, Step 6 of the Agent Eval Dashboard plan) immediately exposed a latent gap in `test/eval-routes.test.ts`'s own `makeBatchRow()` fixture: it predates migration 0021 and never sets `systemPromptSnapshot`, so the field is `undefined` at serialize time against `EvalBatch.system_prompt_snapshot: z.string().nullable()` (nullable, not nullish) — `fastify-type-provider-zod` throws a 500 with no client-visible detail (route handler itself never throws; the failure is in the serializer, invisible from a debugger stepping through the handler). Any NEW route in this file that's the first to add a real response schema over `EvalBatch`/`EvalBatchDetail` must pass `systemPromptSnapshot` (or any other field added by a migration after the shared fixture was authored) explicitly in its own test's row overrides — do not assume an existing fixture used successfully by schema-less routes is already response-schema-safe. `server/test/eval-routes.test.ts` (`makeBatchRow`), `server/src/modules/eval/routes.ts`
- **2026-07-06 [Mistake]** — `AnthropicProvider.doComplete`/`completeStructured` hardcoded `temperature` in every `messages.create()` call. Newer models (`claude-sonnet-5`, `claude-fable-5`) reject it with a `400 invalid_request_error: "temperature is deprecated for this model"`; `claude-haiku-4-5` still accepts it — so the failure is silent for some models and total for others, breaking every review/eval that selects a newer model. There is no reliable way to know in advance which models deprecate the param (Anthropic doesn't expose it via `models.list()`), so a hardcoded model-allowlist would just re-break on the next new model. Fix: a private `createMessage()` wrapper around `client.messages.create` that catches the error, checks `err.status === 400 && /temperature/i.test(err.message + JSON.stringify(err.error))`, and retries ONCE with `temperature` deleted from params — self-heals for any future model with the same deprecation, no model list to maintain. Any Anthropic SDK error param considered "always safe to hardcode" (temperature, top_p, etc.) should get the same detect-and-strip treatment rather than a version/model check, since Anthropic silently varies accepted params per model generation. `server/src/adapters/llm/anthropic.ts`
- **2026-07-07 [Mistake]** — `ValidationError` (`src/platform/errors.ts`) is constructed with `statusCode = 422`, not 400 — `class ValidationError extends AppError { constructor(...) { super('validation_error', message, 422, details); } }`. A plan/task description that says "→ 400 with ApiErrorBody shape (ValidationError)" is simply wrong about the number; the actual HTTP status any `ValidationError` throw produces via `app.ts`'s `setErrorHandler` (`reply.status(err.statusCode)...`) is 422. Verified for `AgentsService.promoteFromBatch`'s cross-agent-batch guard (`POST /agents/:id/evals/promote`) but applies to every `ValidationError` throw project-wide. Before asserting a specific numeric status code for a `ValidationError`-driven route test, check `errors.ts` directly rather than trusting a plan's stated number. `server/src/platform/errors.ts:25-29`, `server/test/agents-promote-route.test.ts`

- **2026-07-06 [Decision]** — Skill-eval has schema + case-CRUD but NO run path, and reusing the L06 agent-eval infra is not drop-in. The `skills` module exposes only eval-case CRUD (`GET/POST/DELETE /skills/:id/evals`, `owner_kind='skill'` rows in `eval_cases`) and the client `SkillDetail/EvalsTab` hardcodes `Run all evals` as `disabled` — no orchestrator/scoring/batch exists for skills. Two blockers to reusing L06: (1) `eval_batches.agent_id` is `NOT NULL → references agents`, so a skill run needs a NEW `skill_eval_batches` table or a generalization (additive only — the NOT NULL column can't be altered); (2) a skill has no model/provider/systemPrompt, so a run must wrap it in a HOST AGENT (agent's systemPrompt + model + this skill's body) via the existing `reviewPullRequest()` — per the user, the model comes FROM the host agent, changed by changing the agent. NOTE the product already owns a skill-eval methodology in `evals/src/{dsl/case.ts (QualityCase = prompt + practices + grounding), scoring/pattern-match.ts, scoring/llm-judge.ts}`: grounding=`patternMatch` (cheap deterministic all-substrings gate) → practices=`llmJudge` (verdict.score ≥ threshold, default 0.6, with per-practice citation results); the in-app skill-eval tab may adopt this instead of the agent-eval `MUST_FIND`/recall-precision model. `server/src/modules/skills/routes.ts`, `client/src/app/skills/_components/SkillDetail/EvalsTab.tsx:57`, `server/src/db/schema/eval.ts:30`, `evals/src/dsl/case.ts:23`
- **2026-07-06 [Decision]** — For a server-side "judge" structured-output call with no prior in-app precedent (skill-eval's `judgePractices`), `LLMProvider.completeStructured({ model, schema, schemaName, messages })` is the ONE correct call shape — confirmed by grepping every existing structured-output call site (`onboarding/service.ts:267`, `reviews/brief-generator.ts:191`, `conventions/extractor.ts:78`, `reviewer-core/review/run.ts:179`, `reviewer-core/intent/classify.ts:35`): all of them pass a plain Zod schema straight to `completeStructured`, never call `toJsonSchema`/`parseWithRepair` at the call site. Those two helpers live ONLY inside the concrete adapters (`server/src/adapters/llm/{openai,anthropic}.ts`, `reviewer-core/src/llm/openrouter.ts`) and inside `MockLLMProvider.completeStructured` (which just `safeParse`s the fixture against `req.schema`) — every module-level caller is fully insulated from JSON-schema conversion and repair-on-malformed-output. Any NEW module needing a structured LLM call should mirror this exact call shape, not reach for `toJsonSchema`/`parseWithRepair` directly. `server/src/modules/skills/eval-scoring.ts`, `server/src/vendor/shared/adapters.ts:88`
- **2026-07-06 [Mistake]** — `SkillsRepository.insertEvalCase` silently dropped `input_meta` — its insert `.values()` never set `inputMeta`, so every skill-eval case (including finding-promoted ones) persisted with `input_meta: null` regardless of what the caller intended. This only surfaces later, at `listCases` read time, as every case showing `source: 'manual'` even for finding-derived cases — no error, no test failure at the write site. Fix: added an optional `input_meta?: unknown` param to `insertEvalCase` (additive, default `null`, existing callers unaffected) so `createCaseFromFinding`'s provenance (`{source:'finding', source_finding_id, source_pr_number}`, AC-8) is actually persisted, not just present in the one synthesized DTO returned from that single call. When a repository `insertX` method's signature doesn't accept a field the service layer needs to persist, check whether the gap is real (silently drops data) before working around it with an in-memory DTO override — the override only fixes the immediate return value, not any later read. `server/src/modules/skills/repository.ts` (`insertEvalCase`)
- **2026-07-06 [Quirk]** — `judgePractices()` (`skills/eval-scoring.ts`) returns only `{score, results}` (the `JudgeVerdict` contract) — no cost/token field. The skill-eval orchestrator can therefore only attribute the REVIEW call's cost (`reviewPullRequest()`'s `outcome.costUsd`) per case for AC-33's "cost attributable per case/batch"; the judge LLM call's own cost is invisible to the caller with the current signature. Do not approximate judge cost by reusing/duplicating `outcome.costUsd` — that silently double-counts the review cost as if it were two separate charges. If per-judge-call cost attribution becomes a real requirement, `judgePractices` needs to additionally return the underlying `StructuredResult.costUsd` from its `completeStructured` call. `server/src/modules/skills/eval-orchestrator.ts` (`runOneCase`), `server/src/modules/skills/eval-scoring.ts` (`judgePractices`)
- **2026-07-06 [Decision]** — `GET /skills/:id/evals` was reshaped from a flat `EvalCase[]` array to `{ cases: SkillEvalCaseListItem[] }` (`SkillEvalCaseListResponse`), delegating to `new SkillEvalService(app.container).listCases(...)` instead of `SkillsService.listEvalCases`. This is a BREAKING response-shape change for any existing client consumer. Confirmed via grep that `client/src/lib/hooks/skills.ts`'s `useSkillEvals()` is the ONLY consumer (`api.get<SkillEvalCase[]>(...)`, expects a bare array) and `EvalsTab.tsx` is the only component reading it — both are STILL ON THE OLD SHAPE as of this commit (Steps 7/8 of the skill-eval-pipeline plan own the client-side fix). Any session touching `client/src/lib/hooks/skills.ts` or `EvalsTab.tsx` before Steps 7/8 land must reshape `useSkillEvals` to unwrap `{ cases }` and consume the new flat `last_run_status`/`last_run_summary`/`last_host_agent_id` fields — the client will otherwise render `undefined`/crash on `.filter()` over a non-array. The EXISTING `POST`/`DELETE /skills/:id/evals[/:caseId]` routes were left untouched (still basic `SkillsService` CRUD, unrelated response shapes). `server/src/modules/skills/routes.ts` (`GET /skills/:id/evals`), `client/src/lib/hooks/skills.ts:139-145`, `client/src/app/skills/_components/SkillDetail/EvalsTab.tsx:23`
- **2026-07-07 [Quirk]** — When walking a real drizzle-orm `SQL` condition tree's `queryChunks` to assert query SHAPE (not just bound `eq()` values — see the existing `extractEqPairs` entry above), a bare raw-text chunk like the one `isNotNull()`/`lt()` emit for the operator itself (`' is not null'`, `' < '`) surfaces as `{value: [' is not null']}` — an ARRAY-wrapped string, not a plain string like a bound `eq()` value (`{value: 'clean'}`). A helper that only checks `typeof v === 'string'` silently sees nothing for these chunks and the assertion fails with a confusing "substring not found in the (correct) SQL" error. Fix: also unwrap `Array.isArray(v)` and check each element. Needed when writing `listTrendBatches`/`previousFullBatch`-style tests that must prove `status IS NOT NULL`/`ran_at < ...` are actually present in the query, since the codebase's usual `extractEqPairs` convention only recovers `eq()` column/value pairs, not the raw operator text of other SQL functions. `server/test/skill-eval-repository.test.ts` (`sqlText` helper)
- **2026-07-07 [Quirk]** — In a multi-agent feature build, two DB migrations CANNOT be generated by parallel implementers: `pnpm db:generate` is stateful (each migration diffs against the PREVIOUS migration's `meta/NNNN_snapshot.json`, so the 2nd must run only after the 1st's snapshot exists) AND both append to the shared `meta/_journal.json`. Running two generate steps concurrently collides the journal and bases the 2nd diff on a stale/absent baseline, corrupting the chain. Serialize them — one implementer doing both in order, or wave-order the second strictly after the first. Separately, applying to a live DB via `db:migrate` is NOT needed for the hermetic gate: `verify:l06` is fully hermetic (typecheck + `eval-scoring`/`eval-repository`/`eval-service`/`eval-routes` mock-based tests), so it stays green without any migration touching a running Postgres. `server/src/db/migrations/meta/_journal.json`, `server/package.json`
- **2026-07-07 [Pattern]** — A genuine bidirectional cross-import between two schema files in `schema/` (here: `eval.ts` already imported `agents` from `./agents`; adding `agent_versions.source_batch_id` then required `agents.ts` to import `evalBatches` from `./eval`) resolves fine at both typecheck and `drizzle-kit generate` time — Drizzle's FK `references: () => table.column` is a lazy callback, not an eagerly-evaluated reference, so JS module-circularity never actually triggers. No reordering or barrel-import indirection needed; just import the table directly from its sibling schema file, same as the existing `agents.ts → skills.ts` precedent.
- **2026-07-07 [Decision]** — Migrations `0021_eval_batch_prompt_snapshot` (`eval_batches.system_prompt_snapshot`) and `0022_agent_version_provenance` (`agent_versions.source` / `source_batch_id` + FK + index) were generated as two purely additive, nullable-column migrations for the Agent Eval Dashboard plan. AT THE SAME TIME, `server/src/vendor/shared/contracts/eval-batch.ts` already carried an uncommitted, out-of-scope edit from an earlier session making `EvalBatch.system_prompt_snapshot` and `EvalBatchCompareResult.deltas.cost_usd` non-optional required fields — this makes `pnpm typecheck` fail at `eval/helpers.ts:187` (`batchDto`) and `eval/service.ts:370` (`compareBatches`'s delta object) REGARDLESS of the schema/migration change (confirmed by stashing the schema edits and re-running typecheck — identical 2 errors either way). Any agent picking up a LATER step of this same plan (the one that updates `helpers.ts`/`service.ts` to populate these new fields) should expect to see and fix these two pre-existing errors — they are not a regression from the migration step. `server/src/vendor/shared/contracts/eval-batch.ts`, `server/src/modules/eval/helpers.ts:187`, `server/src/modules/eval/service.ts:370`
- **2026-07-07 [Pattern]** — `eval-repository.test.ts`'s existing `selectChain`/`makeSelectOnlyDb` fake resolves EVERY `select()` call in a test to the SAME single row array — it cannot express a repository method that issues multiple sequential `select()` calls each needing a DIFFERENT row set (e.g. `listEvalConfiguredAgentSummaries`: case-count groupBy → agents → latest-batch, three distinct shapes in one method; `listRecentBatchesAcrossAgents`: batches-join-agents → eval_runs pass/total aggregate). Reusing the single-resolve fake for these silently returns the SAME rows to every query, making assertions pass for the wrong reason. Fix: a `makeSequencedSelectDb(rowSets: unknown[][])` fake that increments a call index on each `select()` invocation and returns `rowSets[callIndex]`, recording `where`/`groupBy`/`innerJoin` per-call so scoping can still be asserted per query. Use this whenever a repository method's correctness depends on chaining 2+ independently-shaped `select()` results, not just repeating the same one. `server/test/eval-repository.test.ts` (`makeSequencedSelectDb`)
- **2026-07-07 [Quirk]** — During the Agent Eval Dashboard multi-agent build, `pnpm verify:l06` had 2 pre-existing failures in `test/eval-routes.test.ts` (`GET /evals/overview` and `GET /evals/recent` both 500 instead of 200) that are ENTIRELY within Step 6's ownership (`server/src/modules/eval/routes.ts`'s response-schema mapping — a parallel implementer's in-flight work at the time Step 5 (`agents/` promote flow) ran its own verification). Confirmed unrelated to `agents/repository.ts`/`service.ts`/`helpers.ts` — the failing tests never touch `AgentsRepository`/`AgentsService` at all (only stub `EvalRepository.prototype`). When running `verify:l06` as part of a LATER or PARALLEL step in a multi-agent plan, check `git status` first to see which files are still mid-edit by other steps before assuming your own change caused a failure — grep the failing test for the module your step actually owns. `server/test/eval-routes.test.ts`, `server/src/modules/eval/routes.ts`
- **2026-07-07 [Decision]** — `POST /evals/run-all`'s concurrency is a TWO-LEVEL cap, not a single global one: `EvalRunOrchestrator.runManyBatchesWithCap` runs at most `CONCURRENCY` (=3) AGENTS' batches in parallel, AND each agent's `executeBatch` independently caps its own cases at the same `CONCURRENCY` (=3) — so the real ceiling is up to 3×3 = **9 concurrent `reviewPullRequest` (LLM) calls**, NOT 3. This is INTENTIONAL and matches the plan's explicit "3 concurrent agents" design + AC-13 (user-confirmed 2026-07-07); the per-workspace rate-limit (2/min, keyed `eval-run-all:${workspaceId}`) is what curbs bursts, not a global LLM-call cap. A code-review reading AC-13 as "≤3 LLM calls total" will flag the 9 as a violation — it is NOT; do not "fix" it to a single global cap without an explicit product decision. A true hard total cap would need a shared semaphore across BOTH levels, not a param tweak. `server/src/modules/eval/run-orchestrator.ts` (`runManyBatchesWithCap`), `server/src/modules/eval/service.ts` (`executeRunAll`)
- **2026-07-07 [Quirk]** — After adding a field to a shared Zod contract (`server/src/vendor/shared/contracts/*`), the already-running `tsx watch` dev server (`:3001`) served responses with the NEW field **silently stripped** — a 200 with the field simply absent, NOT a 500 — even though a sibling edit in `src/modules/eval/repository.ts` (a `kind='full'` filter) HAD taken effect in the same process. Root cause: `fastify-type-provider-zod` strips output keys the response schema doesn't know, and the running process was still on the OLD contract because `tsx watch` did not restart on the `@devdigest/shared` path-aliased contract edit (edits under `src/modules/` do trigger it). Symptom on the client: `vundefined` / `latest_version=undefined`. Diagnosis that saved time: a throwaway `tsx` script (`createDb(devUrl)` + call the repo method directly) confirmed Drizzle mapped the new columns correctly (`row_number() over (...)::int` → JS `number`), proving the query/mapping were fine and isolating the fault to the stale in-process contract. Fix: **manually restart `:3001`** after a shared-contract change; don't trust `tsx watch` to reload `vendor/shared`. `server/src/vendor/shared/contracts/eval-batch.ts`, `server/src/modules/eval/routes.ts`
- **2026-07-07 [Pattern]** — SUPERSEDED 2026-07-07 (the Eval Dashboard's per-run version is now the PROMPT version via a JS fold, no longer a `row_number` ordinal — see the entry below). Original: Deriving a per-agent "version" ordinal (v1..vN) for the Eval Dashboard without a stored column or migration: a window function `sql<number>\`(row_number() over (partition by ${agentId} order by ${ranAt} asc, ${id} asc))::int\`.as('version')` computed over the `kind='full'` set BEFORE the outer `ORDER BY ran_at DESC … LIMIT` gives the true historical ordinal even though only the most-recent N rows are returned (window functions run before LIMIT). The latest full batch's version is just the per-agent COUNT of full batches (reduced in JS from the batch rows). Derived-on-read renumbers automatically when history is cleared — no migration, no drift. Inline `sql` in a mixed `.select({ table, scalar })` maps by object key; `.as('name')` + parens around the cast is the safe form. `server/src/modules/eval/repository.ts` (`listRecentBatchesAcrossAgents`, `listEvalConfiguredAgentSummaries`)
- **2026-07-07 [Decision]** — The Eval Dashboard's per-run `version` (recent feed) and `latest_version` (agent summary) were changed from a run/attempt ordinal (`row_number()`) to the PROMPT version — it bumps only when an agent's `system_prompt_snapshot` text changes, so repeated runs on an unchanged prompt share one vN and pre-0021 batches (null snapshot) have NO version. Computed as a JS fold `promptVersionsByAgent` (server mirror of the client `promptVersionMap`), NOT a SQL window, for two reasons: (1) the "previous NON-NULL snapshot" null-skipping the fold needs is fragile to express with `lag()` over interspersed nulls; (2) `eval-repository.test.ts`'s fake dbs never execute SQL, so a `row_number()`/window value is a black box the unit tests can't verify, whereas a JS fold IS exercised by them (a genuine inversion of the usual "push it into SQL" instinct). Field NAMES were KEPT (`version`/`latest_version`) and only their MEANING + doc comments changed — a rename to `prompt_version` would have churned both `@devdigest/shared` mirrors + every client/server fixture for zero behavior gain. `EvalRecentBatchRow.version` became `.nullable()` (a shared-contract change → **restart :3001**, see the tsx-watch quirk above). `listRecentBatchesAcrossAgents` now issues a THIRD `select()` (each window agent's full-batch snapshot history) because a batch's prompt version depends on its agent's ENTIRE snapshot lineage, not the capped 25-row window — folding over just the window would under-count once an older snapshot-bearing batch falls outside it; this shifts every `makeSequencedSelectDb` rowset index in the recent-feed repo tests. `server/src/modules/eval/repository.ts` (`promptVersionsByAgent`, `listRecentBatchesAcrossAgents`, `listEvalConfiguredAgentSummaries`)

- **2026-07-08 [Quirk]** — Running Stryker on the skill-eval / eval repositories (`pnpm mutate --mutate src/modules/skills/eval-repository.ts` → 82.35%, 6 survived) — which survivors are REAL test gaps vs. EQUIVALENT mutants is dictated by the codebase's call-recording hermetic-fake convention, so the same triage applies to every repo tested this way. (1) A `StringLiteral` mutant inside a scoping `eq(col, 'literal')` — e.g. `eq(evalCases.ownerKind, 'skill') → ''` in `getCase` — SURVIVES because `makeSelectOnlyDb`/`selectChain` return the seeded rows regardless of the WHERE clause; a test asserting only `calls.where.length === 1` never inspects the literal. Kill it by extracting eq-pairs from the recorded condition (`extractEqPairs(calls.where[0]![0]).owner_kind === 'skill'`), exactly as `listTrendBatches`/`previousFullBatch` tests already do. (2) `.rejects.toThrow()` with NO arg lets `ConfigError('insertRun: insert returned no row') → ''` survive — pass the message substring (`.rejects.toThrow('insert returned no row')`). (3) `ObjectLiteral` mutants on `.select({ id }) → ({})` and `.returning({ id }) → ({})` in `clearHistory` are EQUIVALENT, not gaps: only `deletedRuns.length`/`deletedBatches.length` are read (and the `inArray` subquery, which the fake resolves by identity), so no hermetic test can observe the projection — do NOT chase them to 100%; the two `.returning({})` ones are equivalent even against real Postgres (RETURNING yields one row per deleted row regardless of projection), and only the `.select({})` one is catchable — but solely via a `*.it.test.ts` against real Postgres, low ROI. Net: killing (1)+(2) reaches ~91% with the remaining 3 documented-equivalent; treat that as effectively 100% of killable mutants rather than adding brittle tests for the metric. `server/test/skill-eval-repository.test.ts`, `server/src/modules/skills/eval-repository.ts`

- ~~**2026-07-10 [Quirk]** — `cd server && pnpm typecheck` fails on a clean checkout of `feat/multi-agent-review` with 8 pre-existing errors, ALL unrelated to any in-flight plan change: `../reviewer-core/src/llm/openrouter.ts`/`structured.ts` report `Cannot find module 'openai'`/`'zod'`/`'openai/helpers/zod'`, plus 3 `Type 'unknown' is not assignable to type 'T'` in the same files and `src/adapters/llm/{anthropic,openai}.ts`. Confirmed via `git stash` (errors identical with and without a working-tree change) — this is an environment/module-resolution issue in `reviewer-core`'s own scope, not a regression from any Multi-Agent Review plan step. Any implementer instance running `pnpm typecheck` as its own Step-3 verification will see these 8 errors regardless of what it touched; don't spend time diagnosing them as self-caused — confirm via `git stash` if in doubt, then report only NEW errors beyond this baseline. `server/tsconfig.json`, `reviewer-core/src/llm/openrouter.ts`, `reviewer-core/src/llm/structured.ts`~~ (superseded 2026-07-10 — root cause was simply `reviewer-core/node_modules` never having been `pnpm install`ed; once that ran (confirmed by the newly-created `reviewer-core/pnpm-lock.yaml`), `cd server && pnpm typecheck` is CLEAN — 0 errors, verified during Step 4 of the same plan. This was a one-time environment-setup gap, not a durable baseline — re-check `reviewer-core/node_modules/openai` exists before assuming the 8 errors still apply.)
- **2026-07-10 [Quirk]** — `diff-loader.ts`'s `loadDiff()` does NOT throw just because `container.git.diff()` throws — its try/catch silently falls through to `diffFromPrFiles()` (the pr_files-patch reconstruction), which itself succeeds (with an empty-files diff) whenever `repo.getPrFiles()` resolves to `[]`. To hermetically simulate a genuine `loadDiff` failure (e.g. to exercise `executeRuns`/`executeRunsConcurrent`'s `failAll` pre-work-failure path), mock BOTH `container.git.diff` to reject AND the `ReviewRepository.getPrFiles` stub to reject — mocking only `git.diff` produces a quietly-successful empty diff instead of the intended failure. `server/src/modules/reviews/diff-loader.ts`, `server/test/multi-run-concurrency.test.ts`
- **2026-07-10 [Quirk]** — `*.it.test.ts` cannot assert a live HTTP 429 for ANY per-route `config.rateLimit` — `src/app.ts` only registers the `@fastify/rate-limit` PLUGIN when `config.nodeEnv !== 'test'`, and every `.it.test.ts` builds its app via `loadConfig({ NODE_ENV: 'test' })`, so route-level rate-limit config is inert (never enforced) in this harness regardless of burst size. `brief-generator.it.test.ts`'s AC-16 block already established the correct substitute pattern (not previously captured here): import and assert the route's rate-limit CONSTANT statically, then fire a burst of calls and assert `every(r => r.statusCode !== 429)` to positively document the env-gate rather than silently having a rate-limit test that can never fail. Reused verbatim for `multi-run.it.test.ts`'s `MULTI_AGENT_RUN_RATE_LIMIT` check. `server/src/app.ts:123`, `server/test/brief-generator.it.test.ts`, `server/test/multi-run.it.test.ts`
- **2026-07-10 [Quirk]** — `multi_agent_runs` (id/workspace_id/pr_id/ran_at) was already a live, migrated table BEFORE the Multi-Agent Review feature's Step 2 ever ran — it's created by `0000_init.sql` itself (verified via `git show HEAD:.../runs.ts` and `grep multi_agent_runs migrations/0000_init.sql`), dormant and unlinked to `agent_runs` since project inception. Step 2 (`docs/plans/2026-07-09-multi-agent-review.md`) therefore only needed to ADD the `agent_runs.multi_agent_run_id` FK column + index — never `CREATE TABLE multi_agent_runs`. A fresh agent seeing `multiAgentRuns` already fully defined in `schema/runs.ts` might assume a peer step already did unrelated schema work; it didn't — the table has simply been provisioned since the initial migration, waiting for this feature to link it. `server/src/db/schema/runs.ts:49-58`, `server/src/db/migrations/0000_init.sql:198`
- **2026-07-10 [Decision]** — `multi-run.repo.ts`'s `getGroupScoped(db, workspaceId, groupId)` is the SOLE workspace-scoping gate for the whole multi-agent-run read path; `listAgentRunsForGroup(db, groupId)` and `findingsAndReviewsForRuns(db, runIds)` deliberately take NO `workspaceId` param and don't re-filter `agent_runs`/`reviews` by it, even though the plan's own "Skills & Patterns" section states "R1 — every new/edited query ... filters `workspace_id`" (read literally, this looks like a violation). Confirmed intentional by cross-referencing the plan's own Step 4 (`multi-run.service.ts`'s `getComposedRun`): it calls `getGroupScoped` first (404s the whole request if the group isn't in the caller's workspace), THEN feeds the now-trusted `groupId`/derived `runIds` into the two unscoped reads — exactly the same "validate once via a scoped parent lookup, then use the trusted child id" pattern the codebase already uses for `review.repo.ts`'s `reviewsForPull(db, prId)` (no `workspaceId` either). Do not "fix" this by threading `workspaceId` into the two downstream functions — it would diverge from the plan's literal signatures for no security gain, since `groupId`/`runIds` are only ever reachable after the `getGroupScoped` gate already passed. `server/src/modules/reviews/repository/multi-run.repo.ts`, `server/src/modules/reviews/repository/review.repo.ts:58`
- **2026-07-10 [Quirk]** — `findings.category` has TWO different validation strictnesses depending which shared contract reads it back, despite the DB column itself being an unconstrained `text('category').notNull()`: `AgentColumnFinding.category` (`observability.ts`, used by the multi-agent view) is a loose `z.string()`, but `FindingRecord` (`review-api.ts`, used by `GET /reviews/:id`) is `Finding.extend({...})` and INHERITS `category: FindingCategory` — the strict enum `['bug','security','perf','style','test']` (`findings.ts:14,50`). A finding inserted with any other category string (e.g. `'architecture'` for an Architecture Reviewer persona) passes the DB insert silently and renders fine in the multi-agent view, but 500s at `GET /reviews/:id` when `fastify-type-provider-zod` tries to serialize it against the strict enum. When seeding or writing findings for a NEW reviewer persona whose natural category isn't one of the 5 enum values, pick the closest existing one (e.g. `'style'` for structural/layering findings) — do not invent a new category string, since `vendor/shared` is do-not-touch and the enum can't be extended without a cross-package contract change. `server/src/vendor/shared/contracts/findings.ts:14`, `server/src/vendor/shared/contracts/observability.ts:26`, `server/src/vendor/shared/contracts/review-api.ts:15`, `server/src/db/seed.ts`
- **2026-07-10 [Mistake]** — `MultiRunService.computeTotalDurationMs`'s "all columns settled" branch computed `max(run.ran_at + run.duration_ms) - group.ran_at`, which UNDERCOUNTS whenever the group's agent count exceeds `MULTI_AGENT_CONCURRENCY_CAP`: `agent_runs.ran_at` is stamped at row-CREATION time (`createAgentRun`, called up front for every job in `startRun`'s loop, BEFORE the concurrency-capped worker pool ever dequeues any of them), while `duration_ms` measures only the job's ACTIVE execution window (`Date.now() - start`, where `start` is captured inside `runOneAgent` at the moment the job is actually dequeued — `run-executor.ts`'s `runWithConcurrencyCap`). A job queued behind the cap therefore has a real finish time LATER than `ran_at + duration_ms` by exactly its queueing wait, which the naive formula silently drops. Fix (no schema change — `agent_runs` has no persisted "dequeued at" timestamp, and adding one needs its own migration, out of scope for a code-review fix round): replay `runWithConcurrencyCap`'s worker-pool schedule via greedy list scheduling using ONLY each run's own `duration_ms` — assign each run, in `ran_at ASC`/creation order (= dispatch order), to whichever of `MULTI_AGENT_CONCURRENCY_CAP` workers frees up earliest, then take the max final worker-free-time as the makespan. This reproduces the pool's real total duration exactly and is IDENTICAL to the old `max(duration_ms)` result whenever agent count ≤ cap (nothing is queued, so no regression for the common case). Reusable pattern: when a bounded-concurrency worker pool's real per-job start/finish wall-clock times aren't persisted but each job's own active duration IS, and the pool's dispatch order is known/derivable, list-scheduling simulation reconstructs the pool's true makespan without any new timestamp column. `server/src/modules/reviews/multi-run.service.ts` (`computeTotalDurationMs`), `server/src/modules/reviews/run-executor.ts` (`runWithConcurrencyCap`, `runOneAgent`)
- **2026-07-10 [Mistake]** — AC-33's "tokens follow cost's null-semantics" gap traced back to an incomplete generalization of an EARLIER fix (see the two 2026-06-27 entries above on `partialCostUsd`/`partialCostKnown`): when the `partialCostKnown` boolean flag was introduced to let the `runOneAgent` catch block distinguish "LLM never returned" (skip write → column stays NULL) from "LLM returned with unknown pricing" (write NULL explicitly), it was applied ONLY to `costUsd` — `partialTokensIn`/`partialTokensOut` kept their plain `number = 0` defaults and were written UNCONDITIONALLY in the catch block, and `completeAgentRun`'s signature declared `tokensIn`/`tokensOut` as required non-nullable `number` (unlike `costUsd?: number | null`), so the write path had no way to express "unknown" for tokens even if the caller wanted to. Result: a genuinely-failed run (LLM never returned) persisted `tokens_in: 0`/`tokens_out: 0` instead of `NULL`, which then defeated `computeTotalTokensIn`/`computeTotalTokensOut`'s already-correct "null if any run is null" logic (`multi-run.service.ts`) since it never saw a null to trigger on. Fix: made `completeAgentRun`'s `tokensIn`/`tokensOut` optional+nullable with the SAME conditional-spread write pattern as `costUsd`, renamed `partialCostKnown` → `partialOutcomeKnown` (it now gates all three fields captured at the same point in the try block), and applied the identical omit-when-pre-work-failed treatment to the `failAll` pre-work-failure path (diff-load failure before any agent ran), which had the same `tokensIn: 0, tokensOut: 0` bug. Lesson: when a "known vs unknown" gating flag is introduced for ONE nullable field captured alongside SIBLING nullable fields in the same try block (all assigned together right after an async call returns), audit whether the flag should gate all of them — a partial fix that only covers the field the original bug report mentioned leaves siblings with the identical latent bug. `server/src/modules/reviews/run-executor.ts`, `server/src/modules/reviews/repository/run.repo.ts` (`completeAgentRun`), `server/test/multi-run.it.test.ts` (case g)
- **2026-07-10 [Decision]** — `agent_runs.multi_agent_run_id`'s FK was changed from `onDelete: 'cascade'` (the Multi-Agent Review plan's own literal Step 2 spec) to `onDelete: 'set null'` during the code-review fix round, and this is a DELIBERATE correction, not a deviation to "fix back". `cascade` created a silent, asymmetric data-loss path: `repos → pull_requests (cascade) → multi_agent_runs (pre-existing cascade, migration `0000_init.sql`) → agent_runs.multi_agent_run_id (cascade) → run_traces (cascade)` meant a real, reachable `DELETE /repos/:id` call hard-deleted ALL multi-agent run history (cost/duration/findings/traces), while the sibling `agent_runs.prId` column (already `set null`) survives the exact same deletion chain, just orphaned. User approved the fix specifically because migration `0024` was still unapplied at review time, so editing `schema/runs.ts` + the migration SQL + `meta/0024_snapshot.json` directly (no `db:migrate` run) was safe with zero live-DB drift (confirmed via `pnpm db:generate` reporting "No schema changes"). A fresh agent reading only the plan document would see `cascade` there and might "restore" it — check this entry first. `server/src/db/schema/runs.ts`, `server/src/db/migrations/0024_multi_agent_run_linkage.sql`
- **2026-07-10 [Quirk]** — Auditing a commit for runtime-isolation leaks (the worktree `3010|3011|5442|devdigest_ci|devdigest_review` grep from the merge handoff) must run on the STAGED DIFF (`git diff --cached | grep -nE ...`), NOT on full committed content (`git show <sha> | grep`): the latter false-positives on (1) planning docs that quote the forbidden patterns verbatim as "never hardcode these" instructions (`docs/plans/2026-07-*.md`), and (2) `_journal.json` Unix-ms timestamps whose digits coincidentally contain the port substrings — `"when": 1780354425323` matches `5442`, `1781133010463` matches `3010`. Neither is a real leak; a genuine leak lives in `.ts`/`.json` runtime config, never in a drizzle migration journal (which is do-not-edit anyway). Filter the grep to code paths or eyeball each hit's file before concluding — and never "fix" a `_journal.json` timestamp. `server/src/db/migrations/meta/_journal.json`
- **2026-07-10 [Mistake]** — Extending a `@devdigest/shared` port interface (e.g. `GitHubClient`) with new REQUIRED methods in one plan step, when the concrete adapter classes that `implements` it (`OctokitGitHubClient`, `MockGitHubClient`) belong to a LATER step's owned paths, makes `pnpm typecheck` fail project-wide the moment the interface step lands — `TS2420 "incorrectly implements interface"` on both classes, plus `TS2739`/`TS2322` at their `container.ts` construction site. This is not a regression to fix; it's an inherent, expected mid-wave state for any multi-agent plan that splits "add port method" from "implement port method" across steps/waves. Don't touch the implementing files if they're outside your owned paths — confirm the errors are confined exactly to files owned by the very next step, then report the gap explicitly instead of either silently claiming a false-green typecheck or scope-creeping into the next step's files to "fix" it yourself. `server/src/vendor/shared/adapters.ts`, `server/src/adapters/github/octokit.ts`, `server/src/adapters/mocks.ts`, `server/src/platform/container.ts`
- **2026-07-10 [Quirk]** — In the `devdigest-ci` worktree, `reviewer-core/` has NO installed `node_modules` at all (confirmed: `cd reviewer-core && pnpm typecheck` reports `"Local package.json exists, but node_modules missing, did you mean to install?"`) even though `server/`/`client/` are fully installed — a fresh `git worktree add` apparently only got `pnpm install` run for the packages actively being worked on. Since there's no monorepo workspace (each package resolves its own `node_modules` independently; confirmed no root `node_modules` either), `cd server && pnpm typecheck` ALWAYS surfaces 5 phantom errors from `../reviewer-core/src/llm/{openrouter,structured}.ts` (`Cannot find module 'openai'`/`'zod'`) regardless of what you're actually working on — pre-existing/environmental, not something a `server/` edit broke. Verify with `cd reviewer-core && pnpm typecheck` in isolation before assuming your own change caused it. Separately, `server/src/adapters/llm/anthropic.ts:172` and `openai.ts:118` show an unrelated pre-existing `StructuredResult<T>`/`parseWithRepair` generic-inference gap (`Type 'unknown' is not assignable to type 'T'`) that also predates any given session's changes. `reviewer-core/` (no node_modules), `server/src/adapters/llm/anthropic.ts:172`, `server/src/adapters/llm/openai.ts:118` **Update 2026-07-10 (Step 2 of Export-to-CI):** these are NOT two independent issues — `cd reviewer-core && npm ci` (the exact fix `scripts/dev.sh` already automates on a fresh clone) resolves BOTH the 5 reviewer-core module-resolution errors AND the 2 `anthropic.ts`/`openai.ts` `StructuredResult<T>` generic-inference errors in one shot; `cd server && npx tsc --noEmit` came back fully clean (exit 0, zero errors project-wide) immediately after. The anthropic/openai errors were a downstream type-resolution cascade FROM the missing `reviewer-core` deps, not a second, truly-separate pre-existing gap. Run the `npm ci` first — it's free (no source change, gitignored) and likely clears both at once before you spend time investigating either as a real bug.
- **2026-07-10 [Decision]** — The Export-to-CI plan's literal Step 3 spec, if implemented exactly as written, creates a CIRCULAR ES module import inside the new `ci/` module: `helpers.ts` (owns `CI_RESULT_ARTIFACT_NAME`, and `composeCiFiles` imports `generateWorkflowYaml` from `workflow.ts`) and `workflow.ts` (spec says its `generateWorkflowYaml` imports `CI_RESULT_ARTIFACT_NAME` back from `helpers.ts`) — a real 2-file cycle, forbidden without exception by `backend-onion-architecture` R6. It IS technically safe at runtime here (ESM hoists imports before top-level code runs, and both circular references are only read inside function bodies, never at module-init time) — but that "it happens to work" argument is exactly the kind of rationalization the skill's rule and this plan's own P1 (no load-bearing workarounds) warn against. Fix: `generateWorkflowYaml` takes `resultArtifactName: string` as an explicit params field instead of importing the constant — `helpers.ts` (which already has `CI_RESULT_ARTIFACT_NAME` in scope) passes it in. Dependency stays one-way (`helpers.ts` → `workflow.ts`), `CI_RESULT_ARTIFACT_NAME` still lives in `helpers.ts` exactly where a later ingest step expects to import it from. Any later step that touches `workflow.ts`/`helpers.ts` should preserve this direction, not "simplify" it back into a direct import. `server/src/modules/ci/helpers.ts`, `server/src/modules/ci/workflow.ts`
- **2026-07-10 [Decision]** — The Export-to-CI plan's `workflow.ts` spec (Step 3) never mentions an `actions/checkout@v4` step anywhere in the plan or its linked spec (`docs/feature-requirements/2026-07-09-ci-agent-runner-pipeline.md`, grepped for "checkout" — zero hits) — but without one, the generated GitHub Actions workflow has no way to ever have `.devdigest/runner/index.js` present on the runner (a job always starts with an empty workspace), making every exported CI workflow non-functional in production. Added `actions/checkout@v4` as the FIRST step, before the runner step, matching this repo's own `.github/workflows/e2e-web.yml` precedent. This doesn't conflict with the plan's own hermetic-test requirements — those only assert the upload-artifact step is positioned AFTER the runner step, never that the runner step is at array index 0. `server/src/modules/ci/workflow.ts` **Update 2026-07-11:** bumped `actions/checkout@v4`→`@v5` and `actions/upload-artifact@v4`→`@v5` (both Node-24 majors). `@v4` targets Node.js 20, which GitHub is deprecating on Actions runners, so every review run emitted a "Node.js 20 is deprecated" annotation (forced onto Node 24 anyway). Crucially, that GHA annotation is a runner/tooling notice, NOT a review finding — it never flows into `devdigest-result.json` or the DevDigest findings breakdown; a GitHub "1 warning" next to a DevDigest "No findings / 0 warning" on the same run is CORRECT, not a mismatch (they measure different things — runner deprecation vs. WARNING-severity code findings). `ci-workflow.test.ts` asserts the exact `uses:` strings, so they flip with the version bump. **Correction 2026-07-11:** the "both Node-24" claim above is WRONG for upload-artifact — `actions/upload-artifact@v5` still declares `node20` and STILL emits the deprecation annotation (confirmed live: a Scrapper-RSS run on `@v5` warned `actions/upload-artifact@v5` targets Node 20, forced onto Node 24). Only `@v6` is `runs.using: node24` for upload-artifact; `actions/checkout@v5` IS node24 (its warning did disappear). So to actually silence the annotation the generated workflow needs `checkout@v5` + `upload-artifact@v6` — the `@v5`/`@v5` pair is insufficient (upload step still un-corrected in `workflow.ts` at time of writing). **Fixed 2026-07-11:** `workflow.ts` now generates `actions/checkout@v5` + `actions/upload-artifact@v6` (both Node 24); `ci-workflow.test.ts` pins `@v6`. Already-installed repos need an "Update CI config" re-export to pick up the new workflow.
- **2026-07-10 [Quirk]** — `server/src/db/rows.ts`'s own header comment lists "ci" among the cross-cutting consumers it anticipates centralizing row types for, but as of the Export-to-CI plan's Wave 2 (Steps 3 and 4, run in parallel, same session) neither `CiInstallationRow` nor `CiRunRow` actually lives there — both were independently defined locally (`export type CiXRow = typeof t.ciX.$inferSelect;`) inside `ci/repository/installations.repo.ts` and `ci/repository/runs.repo.ts` respectively, since `db/rows.ts` was outside both steps' owned paths. Later steps (7 `export-service.ts`, 8 `ingest-service.ts`, 11 `routes.ts`) should import these two row types from their owning repository files, not expect them pre-centralized in `db/rows.ts` — the comment there describes an aspiration, not the current state. `server/src/db/rows.ts`, `server/src/modules/ci/repository/installations.repo.ts`, `server/src/modules/ci/repository/runs.repo.ts` **Update 2026-07-10 (Step 7 `export-service.ts`):** `AgentRow` is the exception — unlike the two Ci*Row types above, it IS already centralized in `db/rows.ts` (pre-existing, from earlier agents-feature work), so `import type { AgentRow } from '../../db/rows.js'` is both available and R6-compliant, even though `backend-onion-architecture` SKILL.md's R6 bullet list literally names only `db/schema`/`db/client` (not `db/rows.ts`) — `db/rows.ts`'s own header comment is the real authority sanctioning it as shared infra for exactly this cross-cutting-row-type case. Check `db/rows.ts` first for any row type before assuming you must define it locally.
- **2026-07-10 [Quirk]** — Postgres unique indexes/constraints treat NULL as distinct from NULL, so `INSERT ... ON CONFLICT (colA, colB, colC) DO UPDATE` can never detect a conflict when one of the target columns is actually NULL on both the new row and an existing row — the insert just silently creates a duplicate row instead of updating, on every call (a general, easily-missed Postgres gotcha, not specific to this codebase). This bites the `settings` table's `settings_ws_user_key_uq` index (`workspace_id, user_id, key`) whenever a setting is workspace-level (`user_id IS NULL` by design — e.g. a `ci_last_checked_at` marker, as the Export-to-CI plan's Step 4 text literally specifies via `ON CONFLICT`): that upsert would accumulate a new duplicate row on every single call instead of updating one. Fix: explicit read-then-write (select-by-key, then `update` or `insert`) instead of `onConflictDoUpdate` whenever the conflict target includes a column that is deliberately NULL for that row. `server/src/modules/ci/repository/runs.repo.ts` (`setLastCheckedAt`), `server/src/db/schema/core.ts:46`
- **2026-07-10 [Quirk]** — `octokit@^4.0.3`'s `actions.downloadArtifact()` has no usable static return type: GitHub's OpenAPI spec for `GET .../actions/artifacts/{artifact_id}/{archive_format}` documents ONLY a `302` response (`content: never`, a `Location` header) plus a `410` — there is no `200` schema anywhere in `@octokit/openapi-types` for this route. Octokit's generated `Endpoints["..."]["response"]["data"]` therefore types as `unknown` (confirmed empirically: forcing `res.data` into a `string`-typed local in a throwaway scratch `.ts` file under `server/src/`, typechecked with the real project tsconfig, produced `TS2322: Type 'unknown' is not assignable to type 'string'` — not a `{url: string}` redirect-descriptor shape). At RUNTIME, Octokit's fetch-based request layer follows the redirect automatically (standard fetch behavior), so `res.data` IS actually the binary zip body (`Buffer.from(res.data as ArrayBuffer)` is correct) — callers never see the 302 or need to `fetch()` a URL themselves. Any future Octokit Actions/artifacts endpoint whose OpenAPI spec lacks a 200 schema will show this same `unknown`-typed-but-actually-binary pattern; verify via the same scratch-file forced-type-error trick before assuming a `{url}` redirect object. `server/src/adapters/github/octokit.ts` (`downloadArtifact`)
- **2026-07-10 [Quirk]** — Resolving a SIBLING top-level package's path (e.g. `agent-runner/` from `server/src/adapters/*`) via `path.resolve(path.dirname(fileURLToPath(import.meta.url)), '../../../../agent-runner')` is correct ONLY for the raw-TS-via-`tsx` runtime mode (`pnpm dev` / `scripts/dev.sh`, this project's only currently-documented/used server runtime). It silently breaks under the compiled `dist/` output (`pnpm build && pnpm start`, listed in `package.json` but not wired into any documented workflow): `server/tsconfig.json`'s `rootDir: ".."` (see this file's own 2026-06-27 rootDir Decision entry) preserves an extra leading `server/` path segment under `dist/` that the raw source doesn't have (`server/src/adapters/runner-bundle/ncc.ts` → `server/dist/server/src/adapters/runner-bundle/ncc.js`), so the SAME 4-level `../../../../` that correctly reaches the repo root from source resolves to `server/dist/agent-runner` (nonexistent) once compiled — 2 more `..` levels are needed in that mode. Existing same-package relative resolvers (e.g. `platform/prompts.ts`'s `join(dirname(...), '..', 'prompts')`) don't hit this because they never escape the `server/src/` subtree, whose internal shape is preserved identically under `dist/` — the break is specific to reaching a path OUTSIDE the current package. Not fixed here (the plan's spec gives the 4-level snippet verbatim and only `pnpm dev` is actually exercised); flagged in case the compiled-build runtime mode is ever adopted. `server/src/adapters/runner-bundle/ncc.ts`
- **2026-07-10 [Quirk]** — `p-queue@^8`'s `PQueue.add<T>(fn, options?)` types as `Promise<T | void>` by default (the `void` covers a timed-out task when `throwOnTimeout` isn't set) — even when the task function's own return type never includes `void` and no `timeout` option is passed at all, TypeScript still infers the union from the OVERLOAD signature, not from whether a timeout is actually configured. `Promise.all(items.map(i => queue.add(fn)))` then fails to assign to a `ResultType[]` array (`TS2322`) purely from this phantom `void`. Fix: pass `{ throwOnTimeout: true }` as `.add()`'s second argument — this selects the OTHER overload (`Promise<T>`, no `void`) and is harmless when no timeout is set (it only matters if one fires, which never happens without a `timeout` option). `server/src/modules/ci/export-service.ts` (`bulkUpdate`)
- **2026-07-10 [Decision]** — `Container` has no `reposRepo` facade (only `agentsRepo`/`reviewRepo`/`skillsRepo`/`evalRepo`/`repoIntel`/`blast`/`contextDocs` are exposed) and `ci/export-service.ts`'s owned path for Step 7 was a single new file (no `container.ts` edit, no new `ci/repository/*.repo.ts` file in scope) — yet AC-1 needs a workspace-scoped "is this repo connected" check. Resolution: a small private method querying `t.repos` directly via `this.container.db` (mirroring `RepoRepository.getById`'s exact predicate) rather than importing `repos/repository.ts` (which would violate module isolation, R6/AP-4). This is the same pattern `blast/repository.ts`'s `getRepoBasics` already uses for the identical need (though that one omits the workspace scope — do not copy that part). Any future module needing a cheap workspace-scoped repo-existence check should do the same, or — if this need recurs a third time — that's the signal to actually add a `container.reposRepo` facade instead. `server/src/modules/ci/export-service.ts` (`getRepoRow`), `server/src/modules/blast/repository.ts` (`getRepoBasics`)
~~**2026-07-10 [Mistake]** — `InstallationsRepository.upsertPublished`'s `ON CONFLICT ... DO UPDATE SET` list (`agentId`/`triggers`/`postAs`/`workflowContents`/`workflowVersion` only) never clears `disconnected_at`. Consequence, discovered while implementing `ExportService.exportInstallation` (Step 7): re-exporting a PREVIOUSLY DISCONNECTED installation for the same agent+repo correctly reuses its frozen `id`/`slug` (the intended behavior), but the row's `disconnected_at` stays set forever — so `listTracked()` (the Step 8 ingest check loop) and `active_count` (`getAgentCiSurface`) keep excluding/undercounting it even though the user just "reconnected" it by re-exporting. No repository method exists to clear `disconnected_at` on an existing row, and fixing this from `export-service.ts` would mean either editing `installations.repo.ts` (a different step's owned file) or a raw Drizzle write bypassing the repository's write ownership (a P1 workaround) — left as a flagged, unfixed gap. Whoever next touches `installations.repo.ts` should add `disconnectedAt: null` to `upsertPublished`'s conflict SET list (safe: a fresh/active installation's `disconnected_at` is already `null`, so this is a no-op for the common case and only matters for the reconnect case). `server/src/modules/ci/repository/installations.repo.ts` (`upsertPublished`), `server/src/modules/ci/export-service.ts` (`resolveInstallationTarget`)~~ (superseded 2026-07-10 — fixed; see the Decision entry immediately below)
- **2026-07-10 [Decision]** — Closed the above gap (plan-verifier completeness-pass finding, AC-47): `upsertPublished`'s `DO UPDATE SET` now unconditionally includes `disconnectedAt: null` — since this method is invoked ONLY to represent a successful publish, any row it touches is by definition no longer disconnected (a no-op for a never-disconnected row; a real un-disconnect on reconnect). The pre-existing hermetic test that exhaustively enumerates the SET clause's exact key set (`'never includes slug...'`, `ci-installations-repository.test.ts`) needed its expected key array updated to add `disconnectedAt` — any FUTURE column added to this SET clause will hit that same assertion and must update the same array; this is the test intentionally being exhaustive, not a bug to work around. A NEW stateful fake `Db` (`makeStatefulFakeDb`, tracking one in-memory row across sequential `insert`(no-conflict)→`update`(disconnect)→`insert`(conflict/reconnect) calls, applying `config.set`'s keys onto the stored row exactly like a real `DO UPDATE SET`) was needed to assert the actual RETURNED row's `disconnectedAt` through a genuine disconnect→reconnect round trip — the file's existing `makeFakeDb` helper only replays one static canned response per test and can't express multi-call state. `server/src/modules/ci/repository/installations.repo.ts` (`upsertPublished`), `server/test/ci-installations-repository.test.ts` (`makeStatefulFakeDb`)
- **2026-07-10 [Quirk]** — `app.ts` only registers `@fastify/rate-limit` when `config.nodeEnv !== 'test'` (intentional — "Disabled under test so integration suites can hammer endpoints via inject()"), so the standard `.it.test.ts` boilerplate (`loadConfig({...process.env, NODE_ENV: 'test'})`) makes every route's `config.rateLimit` completely inert. A genuine 429 integration test needs a SEPARATE app-builder passing `NODE_ENV: 'development'` (+ explicit `LOG_LEVEL: 'silent'` to skip the `pino-pretty` dev transport) while still reusing the SAME Testcontainers `db` — confirmed safe: the only other `nodeEnv==='test'` branches are the default log level and an unref'd 15-minute reap cron. `server/src/app.ts:104,121-125`, `server/test/ci-routes.it.test.ts` (`makeRateLimitedApp`)
- **2026-07-10 [Decision]** — `ExportService.getAgentCiSurface` (Step 7, frozen) never throws `NotFoundError` for a missing/cross-workspace `agentId` — it silently returns empty aggregates (`listByAgent`'s workspace-scoped filter just yields `[]`), unlike every other method in that file. Since `ci/routes.ts` (Step 11) cannot edit `export-service.ts`, `GET /agents/:id/ci` adds its own `container.agentsRepo.getById(workspaceId, id)` existence guard before delegating — mirrors `reviews/routes.ts`'s `POST /repos/:id/review-all` direct-lookup-before-delegating precedent for the identical kind of gap. `server/src/modules/ci/export-service.ts` (`getAgentCiSurface`), `server/src/modules/ci/routes.ts`
- **2026-07-10 [Mistake]** — `ExportService.exportInstallation` resolved `repoRow` (proving `repoId` belongs to a connected repo in this workspace) but then used the independent, client-supplied `input.repo` body string for every actual identity-critical use downstream: the GitHub calls (`parseRepoFullName(input.repo)` feeding `commitFiles`/`findOpenPr`/`openPullRequest`) and the persisted `ci_installations.repo` column — `repoRow.fullName` was never compared against or substituted for `input.repo` past the initial existence check (plan-verifier completeness-pass finding, AC-1). Consequence: a caller could pass a valid `repoId` for one connected repo paired with an arbitrary/mismatched `input.repo` string naming a different (possibly unconnected) repo, and the server would commit files/open a PR/persist against that unchecked string. `previewFiles` in the same file was already correct — its input schema (`CiExportPreviewInput`) has no `repo` field at all, so it can only ever derive identity from `repoRow.fullName`. Fix: mirror that pattern exactly in `exportInstallation` — after resolving `repoRow`, use `repoRow.fullName` (never `input.repo`) for `resolveInstallationTarget`, `parseRepoFullName`, and `upsertPublished`'s `repo:` field; `input.repo` stays in the frozen `CiExportInput` wire schema but is no longer read for anything identity-critical. General lesson for future review: whenever a service resolves/validates a row from an ID route-param but the request body ALSO carries an independent string field naming the same resource, grep every downstream use of that body field specifically — a mismatch is invisible in normal operation (the shipped UI always sends matching values) and only surfaces as an exploitable trust-boundary gap, never as a test failure or a runtime error. `server/src/modules/ci/export-service.ts` (`exportInstallation`)
- **2026-07-10 [Quirk]** — `execFile('pnpm', [...])` (no `shell: true`) throws on Windows: `pnpm` resolves to a `.ps1`/`.cmd` shim (confirmed via `Get-Command pnpm` → `pnpm.ps1` under `C:\Program Files\nodejs\` on this machine), and Node's `execFile`/`spawn` without a shell can only launch a native binary or a script with its own shebang — `.bat`/`.cmd`/`.ps1` require `cmd.exe`/`powershell.exe` to run at all (documented Node.js child_process behavior, not project-specific). `NccRunnerBundler.build()` (Export-to-CI plan, Step 2) originally called `execFile('pnpm', ['--dir', agentRunnerPath, 'run', 'build'], ...)` — this fails on every Windows dev machine every time the "Install"/"Update CI config" action actually runs, invisibly to every test the plan specifies (hermetic tests explicitly never invoke the real `.build()`; integration tests override the container with `MockRunnerBundler`), so it would only have surfaced at the final manual smoke test. Fix: bypass the `pnpm` wrapper entirely — invoke the target tool's own JS CLI entry via `process.execPath` (always a native binary, identical across platforms): resolve `agent-runner`'s `ncc` devDependency's published `bin.ncc` entry (`node_modules/@vercel/ncc/dist/ncc/cli.js`) and `execFile(process.execPath, [nccCli, 'build', 'src/index.ts', '-o', 'dist'], { cwd: agentRunnerPath })` — preserves the exact same no-shell/fixed-argument-list security posture (OWASP A06), just targeting a plain `.js` file instead of a shell-dependent wrapper script. Verified by running the exact replacement command directly on this machine (exit 0, produced `dist/index.js`). Any future `execFile`/`spawn` of a bare npm-ecosystem command name (`pnpm`, `npm`, `npx`, `tsc`, etc.) without `shell: true` will hit this same Windows failure — prefer resolving to the tool's own JS entry point via `process.execPath` instead of the CLI wrapper name. `server/src/adapters/runner-bundle/ncc.ts`
- **2026-07-10 [Decision]** — Bugfix pass (race condition + disconnect-takeover): `InstallationsRepository.upsertPublished` now includes `slug` UNCONDITIONALLY in its conflict `SET` clause (previously deliberately omitted to keep it frozen — see the entries above). The "frozen slug across a same-agent rename" invariant moved from the REPOSITORY (enforcing it by omission) to the SERVICE layer (`ExportService.resolveInstallationTarget` computing the correct value: the existing row's own slug for a same-agent re-export/reconnect, a FRESH `slugify(newAgentName)` only when a DIFFERENT agent takes over a previously-disconnected installation — a confirmed product decision that a disconnected installation is a fully free slot, not a permanent lock). This reverses a prior hermetic test's literal assertion (`'never includes slug in the conflict SET clause'`) — that test was renamed and its expectation flipped to assert slug IS always included; don't treat the old assertion as ground truth if referenced anywhere stale. `server/src/modules/ci/repository/installations.repo.ts` (`upsertPublished`), `server/src/modules/ci/export-service.ts` (`resolveInstallationTarget`)
- **2026-07-10 [Pattern]** — Testing a Drizzle `onConflictDoUpdate({ setWhere })` guard hermetically (no Testcontainers) requires a GENUINE SQL-tree evaluator, not a scripted stub, or the test can pass even if the real `setWhere` is deleted from the source. Reverse-engineered runtime shapes directly from `node_modules/drizzle-orm/sql/expressions/conditions.js` + `sql/sql.js` (undocumented at the type level): `eq(col, val)` → `SQL{queryChunks:[StringChunk(''), Column, StringChunk(' = '), Param(val), StringChunk('')]}`; `isNotNull(col)` → `SQL{queryChunks:[StringChunk(''), Column, StringChunk(' is not null')]}`; `or(condA, condB)` → `SQL{queryChunks:[StringChunk('('), SQL{queryChunks:[condA, StringChunk(' or '), condB]}, StringChunk(')')]}` — i.e. `or`/`sql.join` nest plain `SQL` objects inside `queryChunks`, so a chunk-list with NO nested `SQL` is a genuine leaf (`eq`/`isNotNull`) and one WITH nested `SQL` is a combinator to recurse into. `Column.name` is the raw DB column name (snake_case); `Param.value` is the raw bound JS value; both are directly importable classes from the `drizzle-orm` package root (`Column`/`Param`/`SQL`/`StringChunk` — all generic classes with type-param defaults, so bare references typecheck with no `<...>`). Extends this file's existing `extractEqPairs`/`sqlText` convention to `or()`+`isNotNull()`, not just `eq()`+`and()`. `server/test/ci-installations-repository.test.ts` (`walkSetWhereCondition`, `evaluateSetWhere`)
- **2026-07-10 [Decision]** — `InstallationsRepository.upsertPublished` now throws `AppError('repo_already_installed', ..., 409)` directly from the REPOSITORY layer when its new `setWhere` guard (`or(eq(agentId, values.agentId), isNotNull(disconnectedAt))`) blocks a conflict-path write and `.returning()` yields no row — a DB-level, atomic second enforcement of the same one-agent-per-repo rule the SERVICE layer (`resolveInstallationTarget`) already checks non-atomically, closing the TOCTOU race between two near-simultaneous exports for the same repo. This looks like `backend-onion-architecture`'s Layer-3 "business rules FORBIDDEN in repository.ts" at first glance, but it is NOT a novel violation — it's the SAME established idiom `RunsRepository.upsertByGithubRunId` already uses in this exact module (`setWhere: eq(workspaceId, ...)` + `throw new ConfigError(...)` on empty `.returning()`, documented in an earlier entry in this file). Before flagging a repository-layer `AppError`/`ConfigError` throw guarding a `setWhere`-blocked conflict as an architecture violation, check whether it's this precedented "DB-level atomic backstop for a race the service layer cannot close alone" pattern — it is, for both cases in `ci/repository/`. `server/src/modules/ci/repository/installations.repo.ts` (`upsertPublished`), `server/src/modules/ci/repository/runs.repo.ts` (`upsertByGithubRunId`)
- **2026-07-10 [Decision]** — `backend-onion-architecture`'s R5 ("`server/src/vendor/shared/` — never add, edit, or delete files here") and the root `AGENTS.md`'s "Do not touch" list both name this path, but the rule's own stated purpose is preventing an UNCOORDINATED edit that desyncs `client/`'s separate physical copy — it is not a blanket ban on ever touching the file. The Export-to-CI plan (`docs/plans/2026-07-09-export-to-ci-plan.md` §3) already established a standing exception for ADDITIVE growth of the `GitHubClient`/`RunnerBundler` interfaces in this exact file, and the original feature build used it to add `RunnerBundler` + 4 `GitHubClient` methods. Confirmed this exception extends to bug-fix follow-up work on the same feature, not just the original build: adding a 5th method (`getDefaultBranch`) the same way (server edit + byte-identical client mirror in the SAME change, both typechecked green) is consistent with the precedent, not a new one-off. Before treating R5 as absolute for a `GitHubClient`/`RunnerBundler`-shaped change, check whether the change is a coordinated, verified, symmetric addition (never a one-sided edit) — that's the actual bar the precedent sets. `server/src/vendor/shared/adapters.ts`, `client/src/vendor/shared/adapters.ts`
- **2026-07-10 [Mistake]** — Testing `IngestService.checkForNewResults`'s per-installation failure isolation with a custom `GitHubClient` subclass whose `listWorkflowRuns` override DELEGATES to `super.listWorkflowRuns(...)` (i.e. `MockGitHubClient`'s single shared `opts.workflowRuns` fixture) for every un-broken repo silently makes TWO DIFFERENT installations observe the IDENTICAL literal `github_run_id`. Since `upsertByGithubRunId`'s `ON CONFLICT` target is `github_run_id` ALONE (no `ci_installation_id` in the arbiter) and `ci_installation_id` is NOT in the update's `SET` list, the second installation's upsert silently updates the FIRST installation's row in place instead of creating a second one — collapsing two intended per-installation test fixtures into one row, surfacing as a confusing `expected [] to have length 1` failure with no obvious cause pointing at the real one (two installations, same run id). Fix: give each installation/repo its OWN distinct run id in a multi-installation hermetic test's mock GitHub client (derive it from `repo.name` inside the override) — never rely on `super.listWorkflowRuns`'s single shared default fixture across more than one installation in the same test. `server/test/ci-ingest-service.test.ts`
- **2026-07-10 [Pattern]** — Extends the `extractEqPairs`/`extractEqValue` real-drizzle-orm-`queryChunks`-walking hermetic-fake-db convention (see this file's 2026-07-06/2026-07-10 entries on that family) to `inArray(column, values)`: its bound value sits at the SAME `chunks[i+2]` position `eq()`'s single `Param` sits at, but as an ARRAY of `Param`-like `{value}` wrappers (confirmed against the installed drizzle-orm@0.38 `inArray`/`sql` source directly — `values.map(v => bindIfParam(v, column))` is pushed as one raw JS array chunk) — `Array.isArray(chunks[i+2])` distinguishes the two cases; map over it to unwrap each `.value`. Also needed genuine RECURSION into nested `queryChunks` (the flat, single-level `extractEqValue` can't see into it), since `and(eq(...), inArray(...))` wraps each operand in its own nested `SQL` object via `sql.join`. Needed to hermetically test a NEW repository method (`RunsRepository.statusesByGithubRunId`) whose whole job is workspace+id-scoped filtering — the codebase's simpler "trust the fixture, don't verify the WHERE clause" fake-db shortcut would have silently let a cross-workspace row leak into the result, making the isolation test pass for the wrong reason. `server/test/ci-runs-repository.test.ts`, `server/test/ci-ingest-service.test.ts` (`findColumnValue`)
- **2026-07-10 [Decision]** — Bugfix pass: `IngestService.runCheckLoop`'s per-installation loop now wraps each installation's ENTIRE body (agent lookup + `listWorkflowRuns` + inner per-run loop) in its own try/catch — completing, one level further out, the exact failure-isolation protection `resolveArtifact` already had for individual runs (this file's earlier 2026-07-10 Decision entry on `resolveArtifact`'s never-throw contract). Before this fix, ONE installation's unreachable GitHub repo (404/403) OR a cross-workspace `github_run_id` collision surfacing as `ConfigError` from `upsertByGithubRunId` (this file's `setWhere`-guard Decision entry) aborted the WHOLE workspace's check — including `setLastCheckedAt` — permanently wedging `/ci/check` for every OTHER installation too, not just the broken one. `setLastCheckedAt` is now reached whenever the loop completes a full pass regardless of individual installation failures; only the OUTER `withTimeout` cutoff still skips it (a genuinely different concern — cut short by budget vs. completed with some installations degraded). Separately, in the SAME pass: `resolveRunValues` now returns `null` (skip, no upsert) for a run whose TERMINAL status + a REAL artifact were already ingested on a prior poll (a `findingsCount != null` check specifically excludes a degraded null-artifact row, which must keep retrying every poll). `CiCheckResult.runs_updated` on a later `/ci/check` call therefore no longer includes every run `listWorkflowRuns` returns unconditionally — only ones ACTUALLY upserted this call, matching the field's literal name and the plan's own "list of rows actually upserted" spec text. Before assuming `runs_updated`'s length tracks 1:1 with the polled window, account for this skip. `server/src/modules/ci/ingest-service.ts` (`runCheckLoop`, `resolveRunValues`), `server/src/modules/ci/repository/runs.repo.ts` (`statusesByGithubRunId`)
- **2026-07-10 [Mistake]** — `NccRunnerBundler.build()`'s `BUILD_TIMEOUT_MS` was `30_000`, mechanically carried over from the plan's Step-8 literal text which reused AC-27's 30 s budget — but AC-27 governs the ingest/CHECK path (`ingest-service.ts`'s `withTimeout`, a user-facing "don't hang the screen" poll), NOT the one-time export BUILD. The real ncc build of `agent-runner` (bundles reviewer-core + openai SDK + zod into one ~1.5 MB file, compiling TS) measured **~52 s** standalone on a loaded Windows dev machine — so every real export died at 30 s with the misleading `ExternalServiceError('Failed to build the CI runner bundle')` (the `execFile` timeout SIGKILLs ncc mid-build; the generic catch hides that it was a timeout, not a build failure). Same test-invisible class as the `execFile('pnpm')` Windows bug two entries below: ALL tests use `MockRunnerBundler`, so the real `.build()` only runs on a live manual export — surfaced exactly there. Fixed by raising to `120_000` (safe margin over observed cost, still bounds a truly-stuck build); there is NO other timeout on the export path (grep-confirmed: `withTimeout` is only in `ingest-service.ts`, and `export-service.ts:316/403` call `runnerBundler.build()` bare), so the single constant fully governs it. If a future export intermittently fails with "Failed to build the CI runner bundle", check the build wall-time against this budget FIRST before suspecting a real build/path error. `server/src/adapters/runner-bundle/ncc.ts` (`BUILD_TIMEOUT_MS`)
- **2026-07-10 [Quirk]** — A worktree pointed at the SHARED dev DB (`localhost:5432/devdigest`, e.g. after copying a full-repo `.env` in to get every lesson's seed repos) can have a STALE `ci_runs`/`ci_installations` from an earlier lesson checkout that predates this feature's migration 0023 — `to_regclass('public.ci_runs')` returns non-null (table EXISTS) yet `GET /ci/runs` 500s with `column ci_runs.workspace_id does not exist` because the OLD table lacks 0023's added columns. Checking table existence is NOT enough; the migration may be un-applied on that particular DB even though the table is there. Fix: just run `cd server && pnpm db:migrate` against the shared DB — 0023 is additive (`ADD COLUMN`/`CREATE INDEX`), applies cleanly on top of the old table, no data loss. Verify with `docker exec devdigest-postgres psql -U devdigest -d devdigest -c "\d ci_runs"` (expect `workspace_id`, `github_run_id`, `repo`, `agent`, `duration_s`, `critical`/`warning`/`suggestion`). `server/src/db/migrations/0023_bitter_changeling.sql`
- **2026-07-10 [Quirk]** — `dockerAvailable()`'s `execSync('docker info', {timeout: 5000})` check (`test/helpers/pg.ts:23-33`) can spuriously return `false` on the FIRST/cold run of `ci-routes.it.test.ts` when launched concurrently with other heavy work (e.g. a parallel subagent) — this file's own cold TS transform+collect phase alone took 74s in one such run (vs. 24s warm on an isolated rerun immediately after), and the 5s docker-info budget is tight enough that concurrent CPU/IO pressure at just the wrong moment pushes it over, even though `docker info` resolves in ~1-2s when checked directly and in isolation. The failure is SILENT: all 10 tests report `skipped`, not failed — easy to misread as "nothing to test" instead of "the availability check itself misfired." Compounding this on Windows: the vitest process then crashed during its OWN post-run worker-pool teardown (`tinypool`'s `ChildProcess.kill()` threw `EPERM`, cascading into `RangeError: Maximum call stack size exceeded` in Node's unhandled-rejection handler), producing a nonzero process exit code even on that clean all-skipped run — don't trust the exit code alone on Windows; read the printed `Tests` summary line instead. Before trusting a "Docker not available" result from this file, re-run it in ISOLATION (no concurrent agents/heavy background work); a real isolated run against this feature took 152s (127s of that inside one `withTimeout`-exercising AC-27 test alone) and passed 10/10, confirming Docker was never actually the problem. `server/test/helpers/pg.ts` (`dockerAvailable`), `server/test/ci-routes.it.test.ts`
- **2026-07-10 [Quirk]** — Live-testing a real `export-ci` (`action:'open_pr'` OR `'files'` — both call `runnerBundler.build()`) while a SECOND full worktree stack is running (e.g. the sibling `devdigest-review` `scripts/dev.sh`: its own Postgres+API+web) can wedge the devdigest-ci API server ENTIRELY: the CPU-heavy ncc build (spawned child) under that contention exceeds even the 120 s `BUILD_TIMEOUT_MS`, and — critically — the `execFile` timeout does NOT reliably kill the ncc child on Windows under load (same `kill EPERM` family as the tinypool teardown crash documented above), so instead of failing cleanly at 120 s the request HANGS and the whole tsx-watch API process stops responding to even a trivial `GET /health` (observed: 3× 20 s timeouts in a row, no error logged, no `ncc` child visible in `Get-CimInstance Win32_Process`). Diagnosis that saved time: a files-export SUCCEEDED (HTTP 200, ~29 s) earlier the same session when load was momentarily lower, and an open_pr against a fake seed repo got PAST the build to its GitHub 404 in 35 s — proving the build code + 120 s budget are correct and the hang is pure machine contention, not a code defect. Fix is operational, not code: only ONE full worktree stack should be live when smoke-testing a real export; pause the sibling `dev.sh` first. Recovery once wedged = restart the API process (a plain `GET` never recovers on its own). POSSIBLE SECONDARY BUG worth a follow-up (not fixed — only manifests under this two-stack contention, never in the demo/CI environment): `NccRunnerBundler.build()`'s `execFile` timeout should force-terminate the child cross-platform (e.g. `killSignal` + an explicit `tree-kill`/`taskkill /T` fallback on Windows) so an over-budget build fails as a clean 500 instead of wedging the event loop. `server/src/adapters/runner-bundle/ncc.ts` (`build`, `BUILD_TIMEOUT_MS`)
- **2026-07-10 [Quirk]** — A live `export-ci` `open_pr` needs a `GITHUB_TOKEN` (server reads it from `server/.env` when `~/.devdigest/secrets.json` is absent) with WRITE scope, and specifically the **`workflow`** permission — NOT just `contents:write` — because the 7 committed files include `.github/workflows/devdigest-review.yml`, and GitHub rejects any API commit that touches `.github/workflows/` unless the token carries `workflow` (classic) / `Workflows: write` (fine-grained). Symptom ladder observed end-to-end against real repos: a fine-grained PAT with only read → `404 get-a-reference` for a repo NOT in its selected list, or `403 "Resource not accessible by personal access token"` at `create-a-tree` for a repo it can READ but not WRITE. Fix that worked: a **classic PAT with `repo` + `workflow`** scopes (`https://github.com/settings/tokens/new?scopes=repo,workflow`), pasted into `server/.env` `GITHUB_TOKEN=`, then FULL server restart (tsx-watch does NOT reload `.env` — dotenv loads once at boot). Verified: PR opened successfully (`felkost/Python_REST_APIs...#4`, 7 files incl. the workflow, 62 s total under low load). The raw octokit error strings surface verbatim in the wizard toast — a clearer "token lacks write/workflow access to <repo>" message is an open UX-polish item. `server/src/modules/ci/export-service.ts` (`exportInstallation` → `commitFiles`), `server/src/adapters/github/octokit.ts`
- **2026-07-11 [Mistake]** — Export-to-CI's `exportInstallation` used to return the LIVE `OPENROUTER_API_KEY` value to the client as `secret_value` (route schema was `CiExport.extend({ secret_value: z.string() })`), and the wizard's Install step rendered it verbatim in a `<code>` block with a "Copy" button — a real credential leak into the browser DOM/clipboard. It was pure "convenience" (the user already HOLDS the key — they set it in Settings — and the workflow only needs the NAME to reference `${{ secrets.OPENROUTER_API_KEY }}`), so echoing the value bought nothing and leaked a secret. Fix: dropped `secret_value` from BOTH the service result (`export type CiExportResult = CiExport`) and the route response schema (`response: { 200: CiExport }`); the Install success view now shows only the PR link + the secret's NAME via the `secretNote` hint (matches the design mockup). Kept the pre-export presence check (`secrets.get('OPENROUTER_API_KEY')` → throw `ValidationError` if unset) but the value is never read beyond that guard. Regression tests assert the value reaches NEITHER the DOM (client `ExportWizard.test.tsx` feeds a hostile `secret_value` and asserts `queryByText` is null) NOR the wire (`ci-routes.it.test.ts`: `secret_value` `toBeUndefined`; `ci-export-service.test.ts`: `hasOwnProperty('secret_value')` false). Do NOT re-add `secret_value` "so users can copy it" — surfacing a live secret you already store is a leak, not a feature. `server/src/modules/ci/export-service.ts` (`exportInstallation`), `server/src/modules/ci/routes.ts`, `client/src/app/agents/[id]/_components/AgentEditor/_components/CITab/_components/ExportWizard/_steps/InstallStep.tsx`
- **2026-07-11 [Quirk]** — An agent whose `model` is not a fully-qualified OpenRouter slug (`vendor/model`) SILENTLY hard-fails every exported CI review. The generated manifest hardcodes `provider: 'openrouter'` and the runner passes `manifest.model` straight to `OpenRouterProvider`, so a bare studio model name (`claude-sonnet-4-6`, or even `gpt-4o-mini` from a `provider:'openai'` agent) is rejected by OpenRouter at runtime with `400 <model> is not a valid model ID` → runner `exit 1`, no artifact written → GitHub run `failure` → `ci_runs.status='failed'`. Invisible in the studio until you open the target repo's Actions log. Guarded now by `assertExportableModel(agent.model)` in `exportInstallation` + `bulkUpdate` (require a `vendor/model` slug; throw `ValidationError` BEFORE any GitHub write) — deliberately a shape guard, NOT a model allow-list (which would go stale on every release). Consequence to know: any `provider:'openai'`/`'anthropic'` agent with a BARE model name (valid for LOCAL studio runs via the direct provider) is now correctly REJECTED for CI export, because CI always runs through OpenRouter — the fix is an OpenRouter slug like `anthropic/claude-3-haiku`. Also: the exported `.devdigest/agents/<slug>.yaml` manifest is a SNAPSHOT frozen at export/bulkUpdate time, so correcting the agent's model in the DB does NOT fix an already-deployed repo until a re-export / "Update CI config" re-pushes it (a stale bad model there keeps failing CI independently of the current DB value). `server/src/modules/ci/export-service.ts` (`assertExportableModel`, `exportInstallation`, `bulkUpdate`)
- **2026-07-11 [Mistake]** — The exported runner hard-fails an ENTIRE review with `[agent-runner] FAILED: Expected exactly one agent manifest in .../.devdigest/agents, found N` whenever a target repo's `devdigest/ci` branch accumulates MORE THAN ONE `*.yaml` manifest — `agent-runner/src/manifest.ts`'s `findManifestPath` requires EXACTLY one (v1 = one agent per repo). This is reachable through the normal UI, not just manual tampering: `Disconnect` HARD-deletes the `ci_installations` row but does NOT remove the already-committed `.devdigest/agents/<old-slug>.yaml` from the branch, and a later export of a DIFFERENT agent to the same repo commits its own `<new-slug>.yaml` via `commitFiles` (additive — it adds/updates blobs, never deletes the prior agent's file), so BOTH manifests coexist on the branch and every subsequent run 500s at manifest load (observed: Scrapper-RSS ended with `performance-reviewer.yaml` + `security-reviewer.yaml` after a Performance→Security agent swap). `exportInstallation`/`composeCiFiles` only ever GENERATE the current agent's file; nothing prunes stale ones, and the one-agent-per-repo guard is enforced only at the `ci_installations` DB level, not on the committed file set. Fix direction: on export, ensure `.devdigest/agents/` ends with EXACTLY the current agent's manifest (delete any other `*.yaml` there in the same commit — needs `commitFiles`/the git tree op to support deletions), since "exactly one manifest" is a hard RUNNER contract, not just a DB invariant. Immediate unblock for a wedged repo: delete the extra manifest (or the whole `devdigest/ci` branch) and re-export a single agent. `server/src/modules/ci/export-service.ts` (`exportInstallation`), `server/src/modules/ci/helpers.ts` (`composeCiFiles`), `agent-runner/src/manifest.ts` (`findManifestPath`) **Fixed 2026-07-11:** `exportInstallation` (open_pr path) now prunes stale manifests — it lists the branch via `getRepoTree(repoRef, CI_BRANCH)`, computes the OTHER manifests with `staleAgentManifests(treePaths, keepPath)` (new pure helper in `helpers.ts`), and passes them as `commitFiles`'s new optional `deletePaths` field, which octokit's `commitFiles` turns into git-tree `sha:null` deletion entries alongside the added blobs — net post-commit state is exactly one manifest. `getRepoTree` failure (missing branch on a fresh export) degrades to "no deletions". `deletePaths` is an ADDITIVE optional on `CommitFilesPayload` (`server/src/vendor/shared/adapters.ts`); the client's partial adapters mirror doesn't define `CommitFilesPayload` at all, so this needed no client-side sync (R5 exception). `server/src/modules/ci/export-service.ts`, `server/src/modules/ci/helpers.ts` (`staleAgentManifests`), `server/src/adapters/github/octokit.ts` (`commitFiles`)
- **2026-07-11 [Decision]** — The CI Runs dashboard status (`ci_runs.status` via `mapGithubRunToStatus`) is DELIBERATELY decoupled from the GitHub Actions check pass/fail. When a run produced a real artifact, the status is derived PURELY from `findings_count` (`succeeded` if >0, `no_findings` if 0) — the GitHub run `conclusion` is IGNORED on that branch. Counterintuitive but correct: a run that surfaces blocking findings trips the `ci_fail_on` gate, so the runner exits non-zero BY DESIGN (to make the GitHub *check* red and block the PR), which GitHub reports as `conclusion: 'failure'`. The original code mapped that to a dashboard `'failed'` (an `if (run.conclusion === 'failure') return 'failed'` line placed BEFORE the findings check) — wrong: it made a SUCCESSFUL review that found e.g. 2 criticals look like a crash. `'failed'` is now reserved for a run that wrote NO artifact at all (hard-crash: invalid model, >1 manifest, diff-fetch error — the `artifact === null` branch, which still honors the GitHub conclusion). Do NOT re-add a `conclusion === 'failure' → 'failed'` short-circuit ahead of the artifact-findings check — that reintroduces the "findings look failed" bug. The GitHub check still blocks the PR (runner exit code unchanged); only the DevDigest dashboard label is decoupled. NB existing rows already ingested as `'failed'` are terminal-skipped by `resolveRunValues` (AC-27 budget) and won't auto-remap — only NEW runs pick up the corrected mapping. `server/src/modules/ci/ingest-helpers.ts` (`mapGithubRunToStatus`)

---
Last updated: 2026-07-11 · Entries: 160
